---
type: capability
tags:
  - daytrader/wiki
  - roadmap
  - maintained-by-llm
updated: 2026-08-23
---

# Daytrader Roadmap

This roadmap collects potential improvements for the Rust daytrader runtime, Hermes self-improvement loop, operations, UX, and long-term maintainability. It is intentionally a planning map, not a commitment list. Prioritize work that improves safety, observability, and reproducibility before increasing automation.

See [urgent-todo](urgent-todo.md) for the short ranked list of items where the system is exposed now. The split is deliberate: urgent-todo holds verified gaps between what the runtime claims and what it enforces, this roadmap holds everything else. An item leaves urgent-todo by landing here in `Recently Landed`.

## Guiding Principles

- Preserve SIM/live separation and keep broker mutations auditable.
- Fail closed on AI, schema, session, market-scope, and broker uncertainty.
- Prefer small, testable changes with clear before/after evidence.
- Let Hermes propose, but keep activation gated through explicit code/config/database controls.
- Keep the wiki as the durable memory for operational lessons, experiments, and strategy behavior.

## Recently Landed

- 2026-08-26: Moved the dashboard Execution tab's Trade Thesis Outcome Evidence card onto a typed Rust observational contract. SSR now receives only aggregate status, recorded/fill coverage, and one/five-session directional-return summaries; raw thesis, fill, close, scan, and safety-marker data remain outside the shared view. Missing or malformed evidence degrades to the existing unavailable state and remains separate from Hermes, configuration, manager gates, and Saxo order mutation.
- 2026-08-26: Moved the dashboard Missed Trade Shadow evidence panel onto a typed Rust observational contract. SSR now receives only aggregate status, sample counts, equal-weighted directional-return summaries, and a per-gate breakdown; raw shadow rows, scan metadata, and the safety marker remain outside the shared view. Missing or malformed evidence degrades to the existing unavailable state and remains separate from manager-gate overrides or Saxo order mutation.
- 2026-08-26: Moved the dashboard Missed Trade Shadows table onto a typed Rust observational contract. SSR now receives only quote-to-quote observation fields and the distinct manager-gate reason; manager-gate audit data and broker payloads remain outside the shared view. Missing or malformed shadow evidence degrades to an empty local list, and the estimates remain separate from gate overrides or order execution while excluding fees, FX, and slippage.
- 2026-08-26: Moved the dashboard Hermes Counterfactual Tracking table onto a typed Rust observational contract. SSR now receives only quote-to-quote observation fields; manager/advice documents and broker payloads remain outside the shared view. Missing or malformed counterfactual evidence degrades to an empty local list, and the estimates still exclude execution, fees, FX, and slippage without creating, placing, cancelling, or modifying a Saxo order.
- 2026-08-26: Corrected the Hermes Overview Proposal Quality Review data path and typed its Rust advisory contract. The Overview now reads the bounded experiment evidence it needs to score, rather than deriving from the Experiments-only list and rendering permanently empty. SSR receives only displayable rubric scores/checks, duplicate counts, and gap labels; persisted experiment/evidence documents remain outside the shared view. Missing or malformed evidence degrades to an empty local list without approving a proposal, changing lifecycle or configuration, queuing, prechecking, or mutating a Saxo order.
- 2026-08-26: Moved the dashboard Hermes One-Variable Audit table onto a typed Rust advisory contract. SSR now receives only audit metadata and bounded display strings for baseline/candidate values; source baseline, overlay, and manager-run JSON remain outside the shared view. Missing or malformed audit metadata degrades to an empty local list without approving, activating, promoting, or rolling back an experiment; changing configuration; queuing; prechecking; or mutating a Saxo order.
- 2026-08-26: Moved the dashboard Hermes Learning Memory table onto a typed Rust advisory contract. SSR now receives only already-redacted lesson status, aggregate observations, cadence, and expiry metadata; raw reflections and internal safety markers remain outside the shared view. Missing or malformed memory evidence, including an unknown lifecycle status, degrades to an empty local list without promoting a lesson, invoking Hermes, changing experiment state/configuration, queuing, prechecking, or mutating a Saxo order.
- 2026-08-26: Moved the dashboard Hermes Lessons Pending Review table onto a typed Rust advisory contract. SSR now receives only already-redacted lesson text and optional period, reflection-summary, and source-session metadata; raw reflection/provider payloads and detailed proposed-action documents remain in the local audit store. Missing or malformed lesson metadata degrades to an empty local list without approving a lesson, invoking Hermes, changing experiment state/configuration, queuing, prechecking, or mutating a Saxo order.
- 2026-08-26: Moved the dashboard Hermes Reflections section onto a typed Rust advisory contract. SSR now receives only reflection time, goal version, summary, aggregate finding/action counts, and optional source-session reference; raw Hermes/provider payloads and detailed findings/actions remain in the local audit store. Missing or malformed reflection metadata degrades to an empty local list without invoking Hermes, changing experiment state/configuration, queuing, prechecking, or mutating a Saxo order.
- 2026-08-24: Moved the dashboard Execution tab's Scheduler Cycles history onto a typed Rust operational contract. SSR now receives only cycle time/status, decision/queue/notification status, and flattened local duration/operations-integrity labels; the retained cycle document, provider detail, and broader diagnostics remain in the local audit store. Missing or malformed evidence degrades to an empty local list without scheduling work, refreshing data, changing a gate, queuing, prechecking, or mutating a Saxo order.
- 2026-08-24: Moved the dashboard Execution tab's flat Order Events list onto a typed Rust lifecycle contract. SSR now receives only event time, local order linkage, event type, optional broker status, and a fixed local failure-stage label; raw Saxo payloads and free-form broker error detail remain in the local audit store. Missing or malformed event evidence degrades to an empty local list without reconciling, replaying, queuing, prechecking, or mutating a Saxo order.
- 2026-08-24: Moved the dashboard Execution tab's Recent Broker Fills list onto a typed Rust reconciliation contract. SSR now receives only fill identity/time, local order linkage, broker-order reference, symbol/side/status, quantity/price/currency, and optional ledger link; raw broker payloads and detailed attribution remain outside the shared view. Missing or malformed fill evidence degrades to an empty local list without reconciling, replaying, queuing, prechecking, or mutating a Saxo order.
- 2026-08-23: Moved dashboard Decision Pulse cards and operations-banner health onto a typed Rust lifecycle contract. The shared SSR view now carries only pulse identity/enablement, compact latest/success/failure report references, and the seven-day attempt count; prompts, provider payloads, and report detail remain on their lazy-loaded paths. Missing or malformed status evidence degrades to explicit unavailable/unknown health without generating a report or changing queues, gates, Hermes, prechecks, or broker authority.
- 2026-08-23: Moved dashboard Held-Position Quiver Conflicts onto a typed Rust advisory contract. The Quiver panel now consumes explicit status, held-symbol count, bearish threshold, review rows, safety, and interpretation fields; unexpected provider detail cannot cross its SSR boundary. Missing or malformed evidence becomes explicit not-loaded or unavailable state without creating an exit, altering a gate, changing Hermes, queues, prechecks, or broker authority.
- 2026-08-23: Moved dashboard latest Markov, Quiver, and daily-indicator run summaries onto a typed Rust lifecycle contract. Operations health and the Markov/Quiver panels now consume explicit availability, retained run identity/time/date/status, coverage, and success/error counts; analysis config/summary documents remain staged JSON. Missing or malformed records degrade to an explicit no-run state without changing collectors, scheduler jobs, Decision Reports, Hermes, gates, queues, or broker authority.
- 2026-08-23: Moved dashboard scheduled-run status onto typed Rust timing contracts. Markov, Quiver, and daily-indicator operations health now receives only availability, enablement, time zone, cadence, and scheduled-target metadata; collector-specific configuration remains outside the dashboard boundary. Missing or malformed schedule metadata degrades to an explicit unknown health state without changing scheduler jobs, provider calls, Decision Reports, Hermes, gates, queues, or broker authority.
- 2026-08-23: Moved the dashboard data-freshness strip onto a typed Rust source contract. Each diagnostic source now carries explicit identity, owner tab, observation time/age, threshold, and state fields through SSR; the existing per-source missing-data degradation remains visible rather than disappearing. This is read-only operational evidence and changes neither scheduler cadence, source refreshes, Decision Reports, Hermes, gates, queues, nor broker authority.
- 2026-08-23: Moved the dashboard AI settings menu onto a typed sanitized Rust contract. It now consumes provider/model provenance and masked key-status metadata directly; unknown raw key fields cannot serialize into the SSR dashboard view. A malformed settings result falls back to config-derived model metadata with an explicit missing-key state, without changing API-key storage, OpenRouter requests, model-selection precedence, Hermes, gates, queues, or broker authority.
- 2026-08-23: Moved the dashboard's header-derived SSO session onto its existing typed Rust contract. The top-bar identity and settings menu now consume the authenticated flag and optional name/email directly; localization persistence remains on its separate compatibility JSON request boundary. This preserves read-only identity display and changes neither ngrok OAuth/SSO handling, user settings, Saxo access, queues, nor broker authority.
- 2026-08-23: Moved the dashboard's sanitized Saxo session status onto a typed Rust contract. The dashboard and Operations panel now use only connection, SIM/LIVE environment, token-validity, expiry, re-auth, and status-text fields; the local session path and credential-adjacent fields no longer cross this UI boundary. Malformed overview data degrades to explicit unavailable evidence without changing OAuth, refresh, SIM/LIVE admission, queues, prechecks, or broker authority.
- 2026-08-23: Moved the dashboard's compact latest-Decision-Report status onto a typed Rust contract. The global health badge and AI Prompts view now consume the local report ID, timestamp, status, model, and error summary directly; detailed normalized/provider report data remains staged JSON in the Decisions/debug paths. Missing or malformed metadata degrades to an explicit no-report state without generating reports or changing Trading Manager, queues, gates, or broker authority.
- 2026-08-23: Moved the dashboard Protective Stop Coverage envelope onto a typed Rust contract. The Execution tab now consumes its coverage status, summary, position/exception/test list boundaries, and safety interpretation directly, while broker- and lifecycle-specific row detail remains staged JSON. A malformed payload degrades to explicit unavailable evidence without invoking Saxo or changing the separate SIM precheck, placement, cancellation, queue, or broker paths.
- 2026-08-23: Moved the dashboard Decision Gate Replay envelope onto its existing typed Rust contract. The Decisions tab now consumes replay status, retained-run count, scenario-list boundary, safety/interpretation, and staged support-risk evidence directly. A malformed payload degrades to explicit unavailable evidence without changing report generation, configuration, Trading Manager gates, queues, or broker authority.
- 2026-08-23: Moved the dashboard Watchlists envelope onto its existing typed Rust contract. Refresh metadata and category-list boundaries now drive the Watchlists tab directly, while evolving quote- and decision-derived category/item detail remains staged JSON. A malformed tab payload degrades to an empty local watchlist without changing quote collection, candidate membership, Decision Report context, Hermes, gates, queues, or broker authority.
- 2026-08-23: Moved the dashboard Trading Manager envelope onto a typed Rust contract. Its stable availability and latest-run boundary now drive Cash Deployment and Instrument Quarantine panels directly, while evolving persisted manager diagnostics remain staged JSON. A malformed overview result degrades to unavailable without changing decision generation, Trading Manager gates, queues, prechecks, or broker authority.
- 2026-08-23: Moved the dashboard Integrity envelope onto a typed Rust contract. Its health state, checked time, acknowledgement count, and warning/mismatch/expiry list boundaries now drive the overview panel and operations banner directly, while check-specific issue detail remains staged JSON. A malformed overview result degrades to an explicit local warning without changing integrity checks, scheduler behavior, Hermes, gates, queues, or broker authority.
- 2026-08-23: Moved the dashboard market-status envelope onto its existing typed Rust contract. Exchange rows retain their staged JSON detail, while the UI and operations health consume typed outer items, summary, scheduler, and price-monitor boundaries directly. A malformed status result degrades to an inactive local view without changing exchange-calendar refreshes, quote monitoring, Hermes, gates, queues, or broker authority.
- 2026-08-23: Moved the dashboard Reconciled SELL Outcomes panel onto the typed local accounting model. Closed-sale rows, symbol/currency/route aggregates, sample state, totals, and explicit holding-time/slippage limitations now render from validated fields; unavailable evidence stays explicit rather than fabricating performance statistics. This adds no provider request, Hermes, gate, queue, or broker authority.
- 2026-08-23: Moved the dashboard Stored Exposure P/L Attribution panel onto a typed stored-Saxo-exposure model. Available and no-snapshot states remain explicit; per-symbol and currency-grouped DKK P/L retains reliability, timestamp, quantity, and instrument-FX fields without claiming realised or isolated FX P/L. This is read-only observability with no provider request, Hermes, gate, queue, or broker authority change.
- 2026-08-23: Moved the dashboard Unrealised P/L Sources reconciliation panel onto a typed local diagnostic model. Dashboard, latest persisted snapshot, and stored Saxo exposure values retain their individual availability, timestamp, source, and instrument-currency FX-basis fields; malformed SSR data degrades explicitly rather than fabricating a comparison. This is read-only observability with no provider request, Hermes, gate, queue, or broker authority change.
- 2026-08-23: Moved the dashboard Repairable Snapshot Evidence panel onto the fully typed retained-snapshot model. Unavailable or malformed SSR evidence now degrades to an explicit unavailable panel, absent composition-change values remain `n/a` rather than becoming zeroes, and historical stored-position semantics remain unchanged. This is read-only local evidence with no Saxo provider, Hermes, gate, queue, or broker authority change.
- 2026-08-23: Moved the dashboard weekly/monthly target cards and since-reset context onto typed goal-tracking fields. Ready versus `pending_baseline` and missing dashboard states remain explicit, so the UI cannot fabricate zero P/L or progress; target and baseline semantics remain unchanged. This is read-only local portfolio-value planning context with no Saxo provider, Hermes, gate, queue, or broker authority change.
- 2026-08-23: Moved the dashboard Performance benchmark panel and proxy rows onto the typed comparison model. Unavailable SSR data remains explicit, and pending proxy-history return/close/timestamp fields stay null rather than becoming fabricated values; freshness labeling and the native-currency price-return caveat are unchanged. This is read-only presentation work with no Saxo collection, Hermes, gate, queue, or broker authority change.
- 2026-08-23: Moved the dashboard Performance summary and confidence badge onto the typed local summary model. Missing or malformed SSR summary data now renders an explicit unavailable state and `n/a` metrics rather than a fabricated zero-value portfolio; the typed context panel retains its separate drawdown/cost-basis evidence. Summary calculation itself is unchanged and remains read-only local account-value context, with no Saxo provider, Hermes, gate, queue, or broker authority change.
- 2026-08-23: Moved the dashboard Performance chart and table onto the already typed local history rows. The state layer validates the complete selected range before rendering; a malformed legacy row degrades the whole Performance history display rather than fabricating zero-value points or a partial range. Existing summary and benchmark computation remains on the same local rows before this UI boundary. This is read-only presentation work with no Saxo provider, Hermes, gate, queue, or broker authority change.
- 2026-08-23: Typed Performance's optional latest benchmark-run record. Its local run identity/time/date/status and reference/success/error counts are now Rust API fields, while no-run/disabled states remain explicit null. It is operational coverage evidence only and does not change proxy collection, performance interpretation, Saxo provider behavior, Hermes, gates, queues, or broker authority.
- 2026-08-23: Typed selected-range Performance history rows. Local aggregate timestamp, snapshot/source provenance, DKK total/invested/cash/cost-basis/unrealised/daily-P&L fields, and position count are now Rust API fields; legacy rows retain an explicit nullable source. These remain local account-value observations including cash, not broker time-weighted returns or live quotes. The dashboard SSR compatibility model and no Saxo provider, Hermes, gate, queue, or broker authority changed.
- 2026-08-23: Typed the read-only Performance benchmark comparison. The selected-range status, baseline/latest timestamps, account-value return, coverage/freshness counts, caveat, and stable configured ETF-proxy rows are now Rust API fields; unavailable proxy-history values remain explicit nulls. Persisted benchmark-run evidence remains compatibility JSON while the read model is staged. This preserves the native-currency price-return proxy limitation and makes no Saxo provider, Hermes, gate, queue, or broker authority change.
- 2026-08-23: Typed Performance goal tracking. Weekly/monthly targets, local-baseline rationale, ready/pending period P/L/target/progress/baseline/start fields, and since-reset return/baseline fields are now Rust API types. Baselines remain scoped to the active import batch and missing evidence remains `pending_baseline`, not fabricated zero progress. This is read-only local portfolio-value context with no provider, Hermes, gate, queue, or broker authority.
- 2026-08-23: Typed the selected-range Performance summary and its confidence provenance. Local-history point/timestamp/value/change/daily-P&L/position/range-return/drawdown fields and confidence status/freshness/source fields are now Rust API fields. It remains a deterministic account-value projection, not broker time-weighted performance or a live quote; history and benchmark details remain staged compatibility JSON, with no provider, Hermes, gate, queue, or broker authority changed.
- 2026-08-23: Completed the typed Performance snapshot-evidence contract. Aggregate/detail integrity tolerance, structural mismatch observations, and broker-derived unrealised-P/L difference observations are now Rust fields alongside the earlier coverage, retained composition, and change evidence. The classification of valuation-method differences versus structural corruption is preserved; this remains local, historical, read-only evidence with no provider, Hermes, gate, queue, or broker authority.
- 2026-08-23: Typed per-symbol retained composition changes inside Performance evidence. Symbol, before/after quantity, quantity delta, and stored DKK market-value/cost-basis deltas are now Rust fields for opened, closed, and resized sets. These remain observational comparisons that include price/FX effects and do not assert trades, fills, or causality; no provider, Hermes, gate, queue, or broker authority was added.
- 2026-08-23: Typed retained per-position rows inside the latest Performance composition. Stored symbol, optional ISIN, currency, quantity, local price, FX, local/DKK cost basis, DKK market value, and recomputed unrealised P/L are now Rust fields. They remain historical snapshot evidence, not live broker positions, and no provider, Hermes, gate, queue, or broker authority changed.
- 2026-08-23: Typed the aggregate-versus-position integrity envelope inside Performance evidence. Integrity status, checked count, structural mismatch count, broker-derived unrealised-P/L difference count, safety, tolerance, and detail rows are now Rust fields. The distinction between valuation-method differences and structural corruption is unchanged, and no provider, Hermes, gate, queue, or broker authority was added.
- 2026-08-23: Typed the retained composition-change envelope inside Performance evidence. Current/previous snapshot metadata, aggregate opened/closed/resized counts, unchanged count, stored DKK movements, and per-symbol movements are now Rust fields. Collecting states remain explicit and this has no provider, Hermes, gate, queue, or broker authority.
- 2026-08-23: Typed the latest-retained snapshot inside Performance evidence. Availability, safety, interpretation, the stored snapshot metadata (identity, time, source, count, and DKK totals), and retained per-position rows are now Rust fields. This preserves collecting-state semantics and does not change provider, Hermes, gate, queue, or broker behavior.
- 2026-08-23: Typed the public Performance snapshot-evidence envelope. Selected-range coverage, retention, timestamps, safety, and nested retained historical diagnostics are now explicit Rust API fields. The response values and read-only behavior are unchanged: no provider, Hermes, gate, queue, or broker authority was added.
- 2026-08-23: Landed Portfolio Snapshot Schema slice 6 stored composition changes. Performance now compares only the two newest retained per-position snapshots inside its selected range, labeling opened, closed, and resized quantities alongside net stored market-value and cost-basis movement. It does not infer trades, fills, or causality from the observation pair; price and FX movement remain explicit. This is a local read-only projection with no provider, Hermes, gate, queue, or broker authority.
- 2026-08-23: Landed Portfolio Snapshot Schema slice 5 retained composition. Performance now shows the latest retained per-position composition inside the selected range, with stored local price, quantity, FX, DKK cost basis, market value, and recomputed P/L. The timestamp and wording make clear that this is historical evidence rather than a live broker portfolio; aggregate chart/valuation semantics remain unchanged. This is a local read-only projection with no provider, Hermes, gate, queue, or broker authority.
- 2026-08-23: Landed Portfolio Snapshot Schema slice 4 evidence coverage. Performance now reports, for its selected range, how many aggregate snapshots retain linked per-position evidence and therefore remain repairable; it explicitly separates the older aggregate-only history rather than falsely claiming coverage. It also exposes the bounded integrity status and retention policy. This is a local read-only Performance projection with no provider, Hermes, gate, queue, or broker authority.
- 2026-08-23: Landed Portfolio Snapshot Schema slice 3 integrity. The scheduler compares a bounded recent set of aggregate and linked-position snapshots with strict count, market-value, and cost-basis tolerances; structural drift appears as `attention_required` in Scheduler Cycles. Broker-derived aggregate unrealised P/L is explicitly separated from recomputed `market value - cost basis`, because that legitimate valuation difference is not data corruption. This is a local read-only diagnostic with no provider, Hermes, gate, queue, or broker authority.
- 2026-08-23: Landed Portfolio Snapshot Schema slice 2 retention. The scheduler keeps full per-cycle `portfolio_position_snapshots` detail for the 90-day drawdown window, then thins older detail to the final stored snapshot per UTC date. Aggregate history is never deleted or rewritten, and this is explicitly a retention point rather than a claim that the retained observation is an exchange close. The scheduler records its prune outcome locally; no provider, Hermes, or broker path is involved. Slice 3 aggregate/detail integrity checks remain a deliberately separate follow-up.
- 2026-08-23: Landed Portfolio Snapshot Schema slice 1. Every new aggregate `portfolio_value_history` row now commits, in the same transaction, a linked `portfolio_position_snapshots` row per effective held position. Each detail row retains quantity, local price, FX rate to DKK, local/DKK cost basis, recomputed market value, and recomputed unrealised P/L, so its DKK figures are independently reproducible. Existing dashboard and performance reads remain on the aggregate table; this adds no broker request, execution mutation, provider call, or new performance claim.
- 2026-08-17: Corrected the SIM execution-admission regression found in live diagnostics. `execution.mode=live` means that the application submits to the Saxo adapter; `saxo.environment` selects the simulated (`SIM`) or real-money (`LIVE`) broker venue. The queue now permits only a valid matching configured/session pair (`SIM`/`SIM` or `LIVE`/`LIVE`) and fails closed for an unknown, missing, or mismatched environment. The dashboard reports `simulated_broker`, `live_broker`, or a specific disabled reason, rather than treating SIM as disabled. Reconciliation remains ISIN-first/case-normalized, and exposure P/L remains converted with each instrument currency's FX rate.
- 2026-08-01: Replaced the public Decision Gate Replay compatibility-JSON envelope with typed Rust `DecisionGateReplayPayload`. Its stable availability, run count, scenarios, safety, interpretation, and support-risk evidence fields are explicit while nested historical-analysis details remain compatibility JSON during the staged port. This does not change target-gate replay calculation, support-risk evidence collection, Decision Reports, Hermes, Trading Manager gates, configuration, protective stops, or Saxo execution. A focused regression pins the serialization. Follow-up: continue only with another small independently tested public contract before attempting broad dashboard-payload conversion.
- 2026-08-01: Replaced the public performance compatibility-JSON envelope with typed Rust `PerformancePayload`. Its stable range key, history list, summary, benchmarks, and goal-tracking fields are explicit while their nested read-model details remain compatibility JSON during the staged port. This does not change performance collection, benchmark retrieval, goal tracking, Decision Reports, Hermes, Trading Manager gates, protective stops, or Saxo execution. A focused regression pins the serialization. Follow-up: continue only with another small independently tested public contract before attempting broad dashboard-payload conversion.
- 2026-08-01: Replaced the public market-status compatibility-JSON envelope with typed Rust `MarketStatusPayload`. Its stable exchange-row, summary, scheduler, and price-monitor fields are explicit while their nested read-model details remain compatibility JSON during the staged port. This does not change exchange-calendar refreshes, market-window calculation, quote monitoring, Decision Reports, Hermes, Trading Manager gates, protective stops, or Saxo execution. A focused regression pins the serialization. Follow-up: continue only with another small independently tested public contract before attempting broad dashboard-payload conversion.
- 2026-08-01: Replaced the public market-watchlists compatibility-JSON envelope with typed Rust `MarketWatchlistsPayload`. Its stable generation time, cache TTL, universe metadata, and categories are explicit while quote/decision-derived rows remain compatibility JSON during the staged read-model port. The existing degraded result remains an empty-category response. This does not change quote collection, candidate membership, Decision Report context, Hermes, Trading Manager gates, protective stops, or Saxo execution. Focused regressions pin both normal and degraded serialization. Follow-up: continue only with another small independently tested public contract before attempting broad dashboard-payload conversion.
- 2026-08-01: Replaced the protected Hermes-experiments compatibility-JSON envelope with typed Rust `HermesExperimentsPayload`. The stable advisory `items` list is now explicit while persisted experiment rows remain compatibility JSON during the staged Hermes read-model port. The Hermes API-key boundary, proposal lifecycle, and activation controls are unchanged. A focused regression pins the serialization. Follow-up: continue only with another small independently tested public contract before attempting broad dashboard-payload conversion.
- 2026-08-01: Replaced the protected Hermes-reflections compatibility-JSON envelope with typed Rust `HermesReflectionsPayload`. The stable advisory `items` list is now explicit while persisted reflection rows remain compatibility JSON during the staged Hermes read-model port. The Hermes API-key boundary, reflection creation, proposal lifecycle, and advisory-only execution boundary are unchanged. A focused regression pins the serialization. Follow-up: continue only with another small independently tested public contract before attempting broad dashboard-payload conversion.
- 2026-08-01: Replaced the public Scheduler compatibility-JSON envelope with typed Rust `SchedulerPayload`. The stable `status` snapshot and `cycles` list are now explicit while persisted scheduler status/cycle rows remain compatibility JSON during the staged scheduler read-model port. The existing degraded-read behavior remains intact: a failed status read returns `null`, and a failed cycle-history read returns an empty list. A focused regression pins the serialization. This does not change scheduler cadence, jobs, Trading Manager, protective stops, Hermes, or Saxo execution. Follow-up: continue only with another small independently tested public contract before attempting broad dashboard-payload conversion.
- 2026-08-01: Replaced the public Execution-tab compatibility-JSON envelope with typed Rust `ExecutionPayload`. The stable `orders`, `fills`, and `events` lists are now explicit while persisted execution rows remain compatibility JSON during the staged execution read-model port. The existing per-list degraded-read behavior remains intact: a failed read still logs a warning and returns an empty list for that collection. A focused regression pins the serialization. This does not change broker synchronization, queue processing, protective stops, Trading Manager gates, Hermes, or Saxo execution. Follow-up: continue only with another small independently tested public contract before attempting broad dashboard-payload conversion.
- 2026-08-01: Replaced the public strategy-journal compatibility-JSON envelope with typed Rust `StrategyJournalPayload`. The stable `items` contract is now explicit while individual persisted journal rows remain compatibility JSON during the staged strategy-learning read-model port. A focused regression pins the serialization. This does not change journal collection, Hermes reflections or proposals, Decision Reports, Trading Manager gates, protective stops, or Saxo execution. Follow-up: continue only with another small independently tested public contract before attempting broad dashboard-payload conversion.
- 2026-08-01: Replaced the public Quiver-signals compatibility-JSON envelope with typed Rust `QuiverSignalsPayload`. The stable `latest_run` and `items` contract is now explicit while persisted Quiver run and signal rows remain compatibility JSON during the staged read-model port. A focused regression pins the serialization. This does not change Quiver collection, scheduler timing, Decision Report or Hermes advisory context, Trading Manager gates, protective stops, or Saxo execution. Follow-up: continue only with another small independently tested public contract before attempting broad dashboard-payload conversion.
- 2026-08-01: Replaced the public Markov-signals compatibility-JSON envelope with typed Rust `MarkovSignalsPayload`. The stable `latest_run` and `items` contract is now explicit while persisted Markov run and signal rows remain compatibility JSON during the staged read-model port. A focused regression pins the serialization. This does not change Markov regime calculation, scheduler timing, Decision Report or Hermes advisory context, Trading Manager gates, protective stops, or Saxo execution. Follow-up: continue only with another small independently tested public contract before attempting broad dashboard-payload conversion.
- 2026-08-01: Replaced the public portfolio-trades compatibility-JSON envelope with typed Rust `PortfolioTradesPayload`. The stable `items` list is now explicit while individual persisted trade rows remain compatibility JSON during the staged portfolio trade read-model port. A focused regression pins the list serialization. This does not change the trade ledger, Saxo portfolio reads, providers, Hermes, Trading Manager gates, protective stops, or Saxo execution. Follow-up: continue only with another small independently tested public contract before attempting broad dashboard-payload conversion.
- 2026-08-01: Replaced the public portfolio-positions compatibility-JSON envelope with typed Rust `PortfolioPositionsPayload`. The stable `total` and `items` list are explicit while individual persisted position rows remain compatibility JSON during the staged portfolio read-model port. A focused regression pins the count/list serialization. This does not change Saxo portfolio reads, providers, Hermes, Trading Manager gates, protective stops, or Saxo execution. Follow-up: continue only with another small independently tested public contract before attempting broad dashboard-payload conversion.
- 2026-08-01: Replaced the public Decision Report list compatibility-JSON envelope with typed Rust `DecisionReportListPayload`. The stable `items` list is now explicit while each persisted provider-shaped report remains compatibility JSON during the staged report-pipeline port. A focused regression pins the list serialization. This does not change report generation, providers, Hermes, Trading Manager gates, protective stops, or Saxo execution. Follow-up: continue only with another small independently tested public contract before attempting broad dashboard-payload conversion.
- 2026-08-01: Replaced the public latest-Decision-Report compatibility-JSON envelope with typed Rust `DecisionLatestPayload`. Polling clients retain the same optional latest persisted report and explicit `null` next-report placeholder, while report rows remain compatibility JSON because they originate in the provider/report pipeline. A focused regression pins the envelope serialization. This does not change report generation, providers, Hermes, Trading Manager gates, protective stops, or Saxo execution. Follow-up: continue only with another small independently tested public contract before attempting broad dashboard-payload conversion.
- 2026-08-01: Replaced the public AI Prompts compatibility-JSON envelope with typed Rust `AiPromptsPayload` and `AiPromptItem` contracts. The fixed operator-facing fields now cover generation time, prompt-surface status, and optional latest Decision Report/Trading Manager compatibility payloads; the latter stay dynamic because they originate in the persisted report pipeline. A focused regression pins the envelope serialization. This does not implement prompt builders, change provider calls, Hermes, Trading Manager gates, protective stops, or Saxo execution. Follow-up: continue only with another small independently tested public contract before attempting broad dashboard-payload conversion.
- 2026-08-01: Replaced the public Saxo authentication-status compatibility-JSON response with a typed sanitized `SaxoAuthStatus` contract. The contract preserves connection, SIM/LIVE environment, token/refresh validity, expiry metadata, re-authentication state, status text, session path, and optional error while excluding OAuth tokens and broker keys by construction. Generic JSON remains only for the extended Session API/dashboard compatibility path. A focused regression pins the public safe fields. This does not change Saxo OAuth, refresh leases, durable-session persistence, session expiry behavior, provider access, Hermes, Trading Manager gates, protective stops, or Saxo execution. Follow-up: continue only with another small independently tested public contract before attempting broad dashboard-payload conversion.
- 2026-08-01: Replaced the public localization compatibility-JSON response with the existing typed `LocalizationPrefs` contract. It continues to resolve header/config defaults and the authenticated operator's stored preferences, preserving locale, time zone, clock, week start, separators, and measurement-system fields. A focused serialization regression pins the public shape. This does not alter persisted preferences, dashboard formatting, Saxo sessions, provider access, Hermes, Trading Manager gates, protective stops, or Saxo execution. Follow-up: continue only with another small independently tested public contract before attempting broad dashboard-payload conversion.
- 2026-08-01: Replaced the public cash-buffer settings compatibility-JSON response with a typed Rust contract for the deployed reserve, deployment ceiling, reinvestment threshold, source, and configuration baseline. Request updates remain non-persistent previews; a focused regression pins that the requested reserve cannot overwrite the published baseline. This does not activate runtime settings, change capital policy, provider access, Hermes, Trading Manager gates, protective stops, or Saxo execution. Follow-up: continue only with a similarly small, independently tested settings or diagnostic contract before attempting broad dashboard-payload conversion.
- 2026-08-01: Replaced the public SSO session compatibility-JSON response with the existing typed `SsoSession`/`SsoUser` contract. Both session route aliases retain the same `authenticated` and optional header-derived user fields; a focused regression covers anonymous and authenticated serialization. This does not change ngrok authentication, locale persistence, Saxo OAuth/session state, provider access, Hermes, Trading Manager gates, protective stops, or Saxo execution. Follow-up: continue only with similarly small, independently tested public contracts before attempting broad dashboard-payload conversion.
- 2026-08-01: Replaced the public runtime-health compatibility-JSON response with a typed Rust model that preserves the stable status, runtime, and build SHA contract. Its focused regression verifies both field values and serialization. It has no application state, provider, Hermes, Trading Manager, protective-stop, or Saxo-execution behavior. Follow-up: prefer the next bounded contract with existing focused regression coverage over broad `state.rs` conversion.
- 2026-08-01: Replaced the Decision Report schema-health endpoint's outer compatibility-JSON response with typed Rust health/issue models in `src/models.rs`. Dynamic JSON remains limited to internal OpenRouter JSON-Schema construction; callers receive only schema name, strictness, issue count, and typed issue details. Regression coverage verifies both the health condition and serialized response contract. This is diagnostics-only: it does not change provider calls, Hermes, Trading Manager gates, protective stops, or Saxo execution. Follow-up: replace the next small, independently tested response boundary rather than widening diagnostics payloads.
- 2026-08-01: Replaced the Decision Report debug endpoint's outer compatibility-JSON response with typed Rust models in `src/models.rs`. The endpoint still returns the same compact `report_id`, metadata, and four redacted/capped diagnostic strings; persisted provider-shaped JSON remains deliberately internal. The focused regression now verifies the typed boundary retains secret redaction and bounded inspection. This is diagnostics-only: it does not change provider calls, Hermes, Trading Manager gates, protective stops, or Saxo execution. Follow-up: replace the next small, independently tested response boundary rather than widening this endpoint's payload.
- 2026-07-31: Completed the seventh M4 read-model extraction. Execution-tab Scheduler history pagination now lives in `src/scheduler_state.rs`, preserving the existing 12-row page size, page clamp, and offset for persisted `scheduler_cycle_history` reads. `AppState` retains query orchestration and payload fields. Scheduler cadence, jobs, retention, notifications, Trading Manager, protective stops, and Saxo execution are unchanged. Follow-up: begin replacing one small compatibility-JSON boundary with a typed model, or extract a similarly pure helper only where it has an existing focused regression test.
- 2026-07-31: Completed the sixth M4 read-model extraction. Execution dashboard pagination and its overview/shared row limits now live in `src/execution_state.rs`, preserving the page clamp, offsets, and page sizes for existing local execution-order reads. `AppState` retains query orchestration and payload fields. Broker synchronization, fill reconciliation, status transitions, Trading Manager behavior, protective stops, and Saxo execution are unchanged. Follow-up: extract a bounded Scheduler projection or begin replacing a small compatibility-JSON boundary with a typed model.
- 2026-07-31: Completed the fifth M4 read-model extraction. Quiver dashboard page bounds now live in `src/quiver_state.rs`, preserving the existing page size, page clamp, and offset while leaving persisted Quiver reads in `src/quiver.rs`. `AppState` retains orchestration and existing payload fields. Quiver collection, subscription handling, scheduler cadence, Hermes context, Decision Reports, Trading Manager gates, and Saxo execution are unchanged. Follow-up: extract a bounded Execution or Scheduler read-model projection with existing API-payload tests preserved.
- 2026-07-31: Completed the fourth M4 read-model extraction. Markov dashboard page bounds now live in `src/markov_state.rs`, and the Markov module now owns its asset summary query used by read-only execution attribution. `AppState` retains orchestration and the existing payload fields; Markov regime calculation, scheduler timing, advisory context, Trading Manager gates, and Saxo execution are unchanged. Follow-up: extract the next bounded Quiver or Execution read-model projection with its API-payload tests preserved.
- 2026-07-31: Completed the third M4 read-model extraction. Pure Performance account-value summary, selected-range return/drawdown, and evidence-confidence projections now live in `src/performance_state.rs`; `AppState` retains history, current aggregate, goal-tracking, and benchmark queries. Existing payload semantics and regression coverage remain unchanged. This is display-only account-value evidence and adds no provider, Hermes, decision, manager, protective-stop, or Saxo-execution behavior. Follow-up: extract a comparably bounded Markov page projection with its existing API-payload tests preserved.
- 2026-07-31: Completed the second M4 Hermes read-model extraction. The one-variable baseline/overlay audit, proposal-quality rubric, duplicate-family vocabulary, and baseline evidence-pack math now live with reflection lessons and learning memory in `src/hermes_state.rs`. `AppState` retains all database queries and experiment/baseline lifecycle writes; existing payload and regression tests remain unchanged. This preserves advisory-only, non-causal evidence semantics and adds no provider, decision, manager, protective-stop, or Saxo-execution behavior.
- 2026-07-31: Started the M4 typed/core module split by extracting deterministic Hermes reflection lessons and expiring learning-memory projections into `src/hermes_state.rs`. The projection preserves existing bounded inputs, redaction, duplicate handling, cadence/status, and TTL semantics; `AppState` retains database reads and lifecycle/mutation operations. No API, provider/prompt, experiment, Trading Manager, protective-stop, or Saxo-execution behavior changed. Follow-up: extract the one-variable audit, proposal-quality, and baseline-evidence pack with the same API-payload tests preserved.
- 2026-07-31: Made AI Prompts a bounded, on-demand inspection surface. The tab now server-renders only Decision Report metadata; opening its debug details fetches four server-sanitized, 4,000-character-capped payloads (prompt, request, provider response, normalized report) and offers local copy controls. Secret-like fields and token-shaped strings are redacted before the browser receives the endpoint response. This is diagnostics-only: it does not alter providers, Decision Reports, Hermes, Trading Manager, sizing, stops, or Saxo execution.
- 2026-07-30: Added a read-only Stored Exposure P/L Attribution panel to Performance. It ranks the bounded current Saxo exposure snapshot by absolute unrealised P/L and groups the same rows by instrument display currency, while preserving Saxo reliability and snapshot evidence. Because Saxo reports exposure P/L in account currency, the currency grouping is explicitly not an FX-attribution claim. This is not realised P/L, sector or strategy-role attribution, a trading input, Hermes context, Decision Report input, sizing, stop, or broker-execution behavior.
- 2026-07-30: Added a read-only Unrealised P/L Sources panel to Performance. It shows the response-time dashboard aggregate beside the latest stored account-value snapshot and the stored Saxo instrument-exposure aggregate, including the broker-account currency, recorded DKK FX basis, timestamp, and the existing integrity tolerance result. The panel avoids calling a stored exposure snapshot real-time and makes no accounting, strategy, Hermes, decision, sizing, stop, or broker-execution change.
- 2026-07-30: Added an explicit read-only account-value confidence label to Performance. It distinguishes a live runtime aggregate, incomplete range history, recent stored history, stale stored history, and an unusable aggregate, while preserving the source/timestamp behind the claim in a tooltip. The label is strictly account-value evidence: it does not certify quote, benchmark, or order freshness and does not enter Hermes, Decision Reports, Trading Manager, sizing, stops, or broker execution.
- 2026-07-30: Completed the next read-only Performance context slice with a since-reset account-value result and selected-range maximum drawdown. “Since reset” uses only the earliest persisted snapshot in the active import batch; absent or non-comparable history remains `pending_baseline`. Drawdown requires two valid values and is explicitly display-only, distinct from the separately enforced trailing drawdown guardrail. No new provider, Hermes, Decision Report, Trading Manager, sizing, or broker input was introduced.
- 2026-07-30: Surfaced read-only weekly and monthly target progress on the Performance tab from the existing portfolio-value history baselines. Missing same-batch history is now explicitly `pending_baseline`, never displayed as zero P/L or zero progress. This is operator evidence only: it does not alter goal values, Hermes context, Decision Reports, Trading Manager selection, sizing, or broker execution. Follow-up: add daily, since-reset, and drawdown cards after defining their independent baseline semantics.
- 2026-07-30: Added explicit benchmark timestamp-alignment labels to Performance and the persisted End-of-Day benchmark table. Each proxy comparison now distinguishes an aligned close from a prior-close or stale-close reference using the stored account and Saxo-proxy dates, so an intraday account value is not presented as a current relative-return comparison. This is read-only display/evidence metadata; it does not enter Hermes, Decision Reports, Trading Manager, sizing, or broker execution.
- 2026-07-30: Rendered the persisted EOD benchmark snapshot as a dedicated End-of-Day table. Operators can now inspect the same aligned proxy returns, excess return, coverage dates, and caveat that Hermes receives, rather than decoding raw journal JSON. The view performs no Saxo/provider call and does not add a trading input.
- 2026-07-30: Moved the read-only benchmark refresh to 22:15 Europe/Copenhagen, after the regular US close and before the 22:30 daily journal. A configuration regression test now requires that ordering, preventing the EOD/Hermes comparison from silently using only the preceding trading day's proxy close. It remains advisory and has no trading or broker effect.
- 2026-07-30: Added a bounded read-only benchmark readthrough to the daily strategy journal. It uses the last stored portfolio value before the local day and the latest value within the day, aligns the same Saxo ETF-proxy closes used by Performance, and retains the full comparability caveat. Hermes can see it only through its existing end-of-day evidence; it does not enter Decision Reports, Trading Manager gates, sizing, protective stops, or broker execution.
- 2026-07-30: Added a daily/weekly Performance comparison through the existing range picker and introduced the read-only `DIA:arcx` Dow Jones Industrial Average proxy alongside SPY, QQQ, and EUNL. Follow-up: validate named broad-Europe and UK index proxies in Saxo SIM before adding them. “Euronext” and “LSE” are venues rather than a single index, so each future reference must identify its underlying benchmark and use a tracked, resolvable instrument.

- 2026-07-30: Replaced the fixed local-clock Quiver schedule with the same Saxo exchange-calendar mechanism used by Decision Reports. Quiver now runs at the shared XNAS/XNYS market opening plus 45 minutes; the US Decision Report remains opening plus 75 minutes, leaving a 30-minute advisory-data window before that report. The context explicitly reports `fresh`, `partial`, `stale`, `missing`, `failed`, `not_due`, or `no_us_session`, so Decision Reports and Hermes cannot present an earlier completed Quiver run as today's evidence. It remains read-only corroboration, never a manager gate or broker mutation.
- 2026-07-27: Backfilled the read-only daily-indicator universe after the support-risk fields landed after the last weekday run. The manual run returned support-risk rows for all 173 currently resolvable assets; 28 unresolved Saxo mappings remain explicit partial coverage. Aligned local and Kubernetes daily-indicator policy at a five-year 1,260-bar target and added a regression test for that subtree, preventing a local run from silently using a shallower history than the deployed support-risk analysis.
- 2026-07-27: Centralized the SIM/LIVE Saxo REST-gateway mapping in the shared HTTP transport and switched OAuth client-context, Markov, portfolio, and order paths to the one fail-closed mapping. Tests pin the two supported URLs and reject unknown environments. This is deliberately URL selection only: session handling, pacing, retries, request ids, parsing, and broker behavior are unchanged.
- 2026-07-27: Retired `strategy.ladder.submit_bracket_with_entry` and `submit_take_profit_after_fill` from shipped configuration and the Rust contract. Neither had an active Rust implementation, while automatic protective stops remain explicitly configured and enforced. The legacy reference defaults remain false when the keys are absent. A future bracket/target feature must first prove Saxo SIM bundled entry-plus-related-order semantics, parent/child state reconciliation, cancel/replace behavior, and unprotected-position handling; it must not reintroduce dormant configuration first. This removes the final unused risk-surface configuration keys from U2.
- 2026-07-27: Began the shared Saxo HTTP-client roadmap slice. Auth, Markov, daily-indicator, portfolio, and order paths now share one process-wide 30-second `reqwest::Client`, allowing connection-pool and HTTP/2 reuse during scheduler sweeps. This transport-only refactor deliberately preserves each call site's existing OAuth/session handling, rate pacing, retries, request ids, response parsing, and broker mutation boundary. Provider, Slack, editorial, and other external clients keep their independently scoped policies. Follow-up: consolidate the duplicated Saxo GET response helpers and add common request correlation after their semantics are covered by shared tests.
- 2026-07-27: Retired the legacy Yahoo ticker map at `strategy.swing.journal.benchmark_indices`. The Rust daily journal never fetched those identifiers, so it could not produce the configured comparison and the map made the deployed configuration misleading. Legacy reference behavior retains its own default map when run separately.
- 2026-07-30: Implemented the first Rust benchmark slice as a read-only Performance feature. `strategy.performance_benchmarks` uses Saxo-resolved `SPY:arcx`, `QQQ:xnas`, and `EUNL:xetr` ETF proxies, backfills their daily closes, and compares each to the selected local account-value range using the latest available close at or before each account boundary. The UI shows coverage dates and does not turn missing data into zero. It explicitly states that DKK account value (including cash) versus native-currency ETF **price** returns is not time-weighted or total-return performance: dividends, FX, fees, tax, and external cash flows are not normalized. The cycle is outside watchlists, Markov, Decision Reports, Hermes, Trading Manager, sizing, and broker behavior. Follow-up: validate the configured proxies against Saxo SIM, then consider an audited broker-performance/FX-normalized comparison before making stronger claims.
- 2026-07-27: Added exchange and currency concentration gates for BUYs. They derive buckets from the canonical symbol exchange suffix and local exchange-to-currency mapping, count persisted positive-quantity holdings plus earlier approved BUYs in the same manager cycle, and preserve adds to an existing symbol without consuming another slot. Both shipped caps start at `0` (explicit unlimited) so this does not silently change the live policy before an operator selects numerical limits; a positive cap fails closed when snapshot or bucket evidence is missing, and a negative value is invalid. The former unsupported sector cap was retired; any future sector control needs a durable source and explicit exposure semantics.
- 2026-07-27: Retired four overlapping inactive position-weight settings: `strategy.swing.min_holding_weight_pct`, `strategy.swing.max_holding_weight_pct`, `strategy.ladder.min_position_weight`, and `risk.max_position_weight`. The active Rust Trading Manager continues to enforce only `strategy.ladder.max_position_weight` (4% by default) against persisted plus same-cycle planned BUY exposure. Legacy reference planning/execution now derive their ceiling from that same key. This is a no-live-policy-change correction that removes conflicting configuration intent; U2 has seven risk-surface keys remaining.
- 2026-07-27: Retired the inactive session-flatten settings. The active strategy explicitly holds through sessions and uses broker-hosted protective stops for adverse moves; it has no scheduled flatten path. Removing `strategy.ladder.session_flatten_enabled` and `flatten_minutes_before_tradable_close` prevents the configuration from claiming a timed exit exists. The legacy reference defaults remain disabled, so this does not modify live Rust/Saxo behavior; U2 has five risk-surface keys remaining.
- 2026-07-27: Added local per-pulse Execution order-state coverage to Decision Pulse Outcome Evidence. Each bounded group now shows the statuses recorded on its local execution orders, such as `executed`, `broker_working`, `broker_expired`, or `execution_failed`, beside read-only BUY/SELL and Hermes-effect evidence. It does not call Saxo to infer a missing result, derive a failure cause, change queue state, or make a broker mutation.
- 2026-07-26: Made Docker Desktop deployment images content-addressed. `make k8s-deploy` tags default API and backup images with the full clean-worktree Git SHA; explicit image overrides remain available. CI now syntax-checks deployment and post-deploy shell scripts, and deployment metadata, Kubernetes image tags, and binary revision identify the same commit.
- 2026-07-27: Extended bounded, read-only Decision Pulse Outcome Evidence with durable per-order Hermes advice effects. Execution now reads the matching `allowed`, `reduced`, or no-op/review-gate effect from the persisted Trading Manager `hermes_advice_delta` snapshot, rather than reconstructing advice from live inputs or raw model rationale. This classifies advice applied to an order but does not establish causal performance impact; blocked/reduced counterfactuals remain separately measured in Hermes. It performs no Saxo, provider, Hermes, configuration, gate, or order mutation.
- 2026-07-26: Retired the inert `strategy.min_selected_assets` breadth floor from both shipped configs, the Rust config contract, and the legacy reference selector. The Rust Trading Manager never enforced it; the legacy selector no longer overrides its own sector-screened selection merely to reach a configured minimum. The real `strategy.max_selected_assets` BUY cap remains enforced. U2 now has 12 risk-surface keys remaining.
- 2026-07-26: Eliminated the last known local-versus-deployed scheduler-policy drift by adding the production `strategy.quiver` cadence to `config.yaml`: enabled weekdays at 19:00 Europe/Copenhagen, a 120-day lookback, and a 60-symbol cap. The Quiver API key remains environment-only. A config-contract regression test now requires the local and Kubernetes Quiver policy blocks to match. The fixed-time cadence was superseded on 2026-07-30 by the Saxo-calendar-aware US-open follow-up, without changing broker behavior.
- 2026-07-26: Made the Trading Manager's six-hour scheduled-report freshness policy explicit in both shipped configs as `strategy.swing.trading_manager.max_report_age_hours`. This preserves current behavior while removing the hidden code-default divergence; a report older than the configured window cannot create new execution orders. The config-contract audit now reports 34 enforced, 30 advisory, 30 unused, 13 risk-surface keys, and seven contracted-but-absent paths.
- 2026-07-26: Retired the final duplicate cash-reserve path, `strategy.swing.cash_buffer_pct`. It is removed from both shipped configs and from the config contract, and startup purges both it and the earlier retired `strategy.capital.cash_buffer` from historical runtime settings. The legacy Python reference also derives its cash reserve from the active `strategy.capital.min_cash_buffer_pct`, so a reference-only run cannot silently use a 10% fallback. Hermes already could not propose the dead variable. U2 now has 13 risk-surface keys remaining.
- 2026-07-26: Made `strategy.max_selected_assets` an enforced per-Decision-Report cap on distinct approved BUY symbols. The cap runs after Hermes and the deterministic technical, cash, risk, holding, allocation, and cost gates; it does not suppress SELLs or a repeat BUY for a symbol already selected by the report. `0` is explicit unlimited mode and a negative value fails the BUY closed. The policy, pre-cap count, and result are retained in Trading Manager/Hermes context and the Candidate Scoring Waterfall explains this separately from the earlier preflight candidate-symbol ceiling. U2 now has 14 risk-surface keys remaining.
- 2026-07-26: Bound the four slow advisory-enrichment scheduler steps so one unavailable external source cannot starve the rest of a cycle. Markov and daily indicators default to four minutes; Quiver and editorial research default to 45 seconds. Operators can tune only within a one-to-900-second range using `SCHEDULER_MARKOV_TIMEOUT_SECONDS`, `SCHEDULER_DAILY_INDICATORS_TIMEOUT_SECONDS`, `SCHEDULER_QUIVER_TIMEOUT_SECONDS`, and `SCHEDULER_EDITORIAL_TIMEOUT_SECONDS`. A timeout is recorded as a structured `timeout` result and retried next scheduler cycle. Broker session/order work, protective stops, decision reports, Trading Manager, execution, and reconciliation remain unbounded: cancelling a broker mutation after it may have reached Saxo would create an ambiguous order state, not a safe failure.
- 2026-07-26: Made `strategy.swing.max_holdings` an enforced concurrent-holdings cap and made Hermes's `constraints.max_positions` truthful. The Trading Manager counts persisted positive-quantity symbols and reserves a slot for each newly approved symbol BUY across the whole scheduler cycle; it blocks a new name once the configured 25-slot ceiling is reached or the position snapshot is unavailable. Adds to an existing holding remain eligible and do not consume another slot. The policy and sanitized counts are stored in manager JSON and sent to Hermes preflight; Hermes cannot relax it. U2 now has 15 risk-surface keys remaining.
- 2026-07-26: Made `strategy.ladder.max_position_weight` an enforced total per-symbol BUY-exposure cap. The Trading Manager combines the persisted DKK value of the current position with BUYs already approved for that symbol in the same scheduler cycle, then limits the new quantity to the remaining headroom under the configured 4% portfolio ceiling. It fails closed when the portfolio or existing-position evidence is missing/invalid, and blocks a BUY when less than one share fits. The sanitized calculation is stored in the manager run, explained in the Candidate Scoring Waterfall, and included in Hermes preflight context; Hermes cannot relax the gate. U2 now has 16 risk-surface keys remaining.
- 2026-07-26: Made `strategy.swing.trading_manager.max_symbols` an enforced per-report symbol ceiling. The Trading Manager keeps the Decision Report's ordered candidate list for audit, but evaluates only the first configured number of distinct symbols; repeated actions for an already-admitted symbol remain eligible and later distinct symbols are persisted as skipped waterfall rows. `0` remains explicit unlimited mode and a negative configuration fails closed. Hermes preflight receives only eligible candidates plus the active cap, so it cannot spend advisory work on symbols the manager will not consider. U2 now has 17 risk-surface keys remaining.
- 2026-07-26: Made `strategy.estimated_slippage_bps` and `strategy.cost_guard_multiple` enforced BUY cost controls. After server-verified price, daily technical, and ATR risk sizing, the Trading Manager now derives the daily indicator's expected target reward in DKK and requires it to exceed a deterministic lower-bound hurdle: exchange-minimum round-trip commission multiplied by the configured guard plus configured one-way slippage. The calculation is persisted in the manager audit/waterfall and exposed to Hermes preflight. It fails closed on missing or model-supplied technical data, invalid configuration, or an unprofitable hurdle. It does not claim to forecast the actual broker commission, FX cost, spread, or fill price. U2 now has 18 risk-surface keys remaining.
- 2026-07-26: Made `strategy.swing.risk_per_trade_pct` an enforced BUY-sizing cap. The Trading Manager now derives per-share risk from the database-verified daily close and ATR14 at the same `strategy.ladder.stop_loss_atr_multiple` used by automatic protective stops, converts that distance through its verified DKK share value, and limits an order to the configured fraction of current portfolio value (1% by default). It downsizes when possible and refuses a BUY when one share exceeds the loss budget, the evidence is missing/model-supplied, or automatic protective-stop maintenance is disabled. The reduced quantity must still clear the commission floor. This is an initial-stop loss estimate, not a promise of execution price through a gap; it makes no Saxo call during gate evaluation. U2 now has 20 risk-surface keys remaining.
- 2026-07-26: Replaced the placeholder after-tax portfolio display with a deterministic, read-only Danish share-income estimate. It reads the configured progressive DKK brackets, applies their incremental tax effect to current-year realised SELL gains plus current unrealised P/L, and labels the dashboard amount as an estimate with its provisional tax. Invalid brackets, a non-DKK configuration, or an unavailable ledger fail open for display with an explicit unavailable status; nothing writes tax to the trade ledger, changes a broker order, or feeds a sizing gate. This removes one risk-surface and one non-risk unused configuration item from U2's audit: 21 risk-surface keys remain.
- 2026-07-26: Reconciled Hermes experiment-variable enforcement to the single published `SUPPORTED_EXPERIMENT_VARIABLES` list. The Trading Manager now uses that list directly when loading a SIM/paper overlay, and accepts only the four variables advertised through Hermes capabilities. The retired `strategy.swing.cash_buffer_pct` alias and two undocumented manager-only paths can no longer alter queue creation; daily/weekly Hermes prompts and operator runbooks now publish the same set. A regression test proves every published variable is loadable and each unpublished path is rejected.
- 2026-07-26: Honored the existing strategy automation switches. `strategy.enabled` now stops new *scheduled* Decision Report submission and Trading Manager queueing; provider requests already submitted are still polled to a terminal audit state. `strategy.swing.trading_manager.enabled` stops creation of new execution orders without abandoning broker synchronization, reconciliation, or protective stops. The Nordic/EU and US follow-up pulse switches suppress their scheduled submissions and surface as `disabled` in Operations instead of appearing stale or missing. Manual report requests remain explicit operator actions. Production defaults remain `true`, and the Kubernetes ConfigMap now states the Trading Manager settings explicitly. This removes four misleading risk controls from U2's inventory: 23 risk-surface configuration keys remain.
- 2026-07-26: Wired `risk.excluded_symbols_csv: ENV:RISK_EXCLUDED_SYMBOLS` into the Rust Trading Manager. The existing `daytrader-env` injection now merges an operator-supplied comma/semicolon/newline-separated list with versioned `risk.excluded_symbols` and `strategy.swing.never_trade_symbols`, trims and case-normalizes every symbol, deduplicates it, and applies the result to both the deterministic gate and its stored Hermes/waterfall context. An empty or missing environment value changes nothing. This removes another misleading U2 risk control: 22 risk-surface keys remain.
- 2026-07-25: Made `strategy.ladder.stop_loss_atr_multiple` real as the first enforcement slice of the protective-stop work. The read-only coverage audit now computes a concrete proposed stop for each position lacking full broker-confirmed coverage — `close - (ATR14 x multiple)` from the latest stored daily indicator row — sized to the uncovered quantity only, and the Execution exceptions table shows the level, its distance from the reference close, and a tooltip explaining the basis. It fails closed: no proposal when ATR14 or close is unusable, or when the computed level is at or below zero. It makes no Saxo call, places nothing, and is explicitly not tick-normalized, since normalization needs Saxo instrument details and happens in the precheck/placement path. Investigation also established that `protective_stop_prechecks` and `protective_stop_lifecycle_tests` are both empty, so the broker path has never executed once; automatic placement stays blocked on an operator running the manual SIM lifecycle test first.
- 2026-07-25: Fixed a permanent false positive in the Overview integrity unreconciled-orders check. All 15 flagged rows were the 2026-05-05 portfolio adoption (`strategy_type = 'portfolio_sync'`, one identical timestamp, 19 rows of which 4 failed instrument resolution). Adopted positions record holdings that already existed at the broker when this system took over the book, so no trade occurred under this system and they can never acquire a trade-ledger row. The check now excludes them and reports `adopted_orders_without_ledger` as a separate count, so the number stays visible without alerting. This had held `healthy` false continuously for roughly three months, training the operator to read the warning as background noise — exactly how a genuine ledger-less fill would have gone unnoticed. The query and its exclusion are now shared constants so the regression test exercises the real SQL rather than a copy.
- 2026-07-25: Realigned the return goal to the operator's actual target of +10-20% per year, settling on +15%. The configured goals had stated three different and much larger things: 20,000 DKK/month (~+115%/yr on a ~304,000 DKK book), 5,000 DKK/week (~+137%/yr), and a Hermes objective of `target_return_30d: 0.47` noted as "10x in 6 months" (~70x the real target). The Hermes number was not merely cosmetic — `promote_only_if.return_30d_gte` used the same 0.47, so no experiment could clear the promotion bar on merit. All five copies now carry `target_return_30d: 0.0117`, 880 DKK/week, 3,800 DKK/month, `goal_version: 2`, and `failure_below_30d_return: -0.02`. The monthly loss floors were rescaled with the goal from -25,000/-50,000 (-8.2%/-16.4% of the book in one month) to -9,000/-18,000 (-3%/-6%), preserving the 2:1 soft/hard ratio. Follow-up: the targets are stored as DKK against a ~300,000 DKK book and drift silently as the portfolio changes; consider deriving them from portfolio value.
- 2026-07-25: Added a read-only config-contract audit (`src/config_contract.rs`). It holds an explicit table classifying every `strategy.*`, `risk.*`, and `taxation.*` key as enforced, advisory, or unused, then reports three kinds of drift: a configured key the table marks unused, a contracted key configuration no longer supplies, and — the drift catcher — a configured key the table does not describe at all. Startup logs a summary and warns per risk-surface key; the Overview integrity payload carries the classified list. Known unused keys are reported as context rather than as a warning so the panel does not sit permanently unhealthy; only genuine new drift raises a warning, scoped by exact key set so an acknowledgement cannot suppress tomorrow's key. Tests pin both shipped configs against the table. It reads configuration and changes nothing.
- 2026-07-25: Added continuous integration (`.github/workflows/ci.yml`). `cargo fmt --check`, `cargo check --all-targets`, and `cargo test` run with warnings-as-errors on push to `main`, on pull request, and on manual dispatch. The workflow is hermetic: no secrets, no Saxo or OpenRouter access, no deploy step. Until now the 324-test suite ran only when an operator typed `make validate`.
- 2026-07-25: Extended the read-only protective-stop coverage audit with explicit operator exceptions and reconciled SIM lifecycle evidence. Only a lifecycle test reconciled as `broker_working` with a broker order identifier can count as coverage; placement-submitted, cancelled, failed, live-environment, or ambiguous test rows remain non-protective. Execution now shows each unprotected or partially protected persisted broker position with quantity, reason, and a non-mutating operator action. This still does not automate a stop, infer parent/child linkage, or change a position.
- 2026-07-25: Added a manually gated SIM protective-stop lifecycle-test workflow. A successful precheck can prepare one isolated `GoodTillCancel` SELL `Stop` placement, with distinct acknowledgement for placement and cancellation plus a separate read-only reconciliation action. Tests are outside `execution_orders`, the scheduler, and Hermes; ambiguous responses never retry automatically. No broker test is created until an operator explicitly confirms the UI placement. Remaining work: perform and review a small-SIM placement/cancel/reconciliation test, then separately design parent/child linkage and unprotected-position exceptions.
- 2026-07-25: Added a read-only protective-stop coverage audit. It compares the latest persisted broker long-position snapshot with local SELL `Stop`/`StopLimit` records, reports protected, partial, planned, and unprotected coverage in Execution, and adds the same bounded sanitized context to Hermes. Only broker-confirmed stop states count as active protection; queued, uncertain, cancelled, or failed stops do not. It makes no Saxo call and never places, replaces, or cancels an order.
- 2026-07-25: Added a read-only multi-horizon support-risk projection to nightly daily indicators. It clusters daily pivot lows from up to five trading years of Saxo OHLC history, reports nearest and next lower support, downside to support, downside after a break, break-risk label, confidence, and returned-history coverage. The same sanitized snapshot reaches the Watchlist tooltip, the Decision Report manager-waterfall/context, and Hermes context. It is not a manager gate, sizing input, order change, or Hermes activation criterion; held-out backtesting remains required before any trading effect.
- 2026-07-25: Added read-only support-risk outcome collection to Decision Reports and Hermes context. It segments stored indicator closes by their recorded low/moderate/high break-risk label and reports next available one- and five-run returns, negative-return rate, confidence, and coverage. It begins in `collecting` state until it has at least 30 completed five-run observations and explicitly does not infer causality, costs, gaps, or a trading rule.
- 2026-07-23: Hardened the lazy TradingView modal close path. Close clicks are handled during the capture phase, immediately hide the active target before removing its URL fragment, and Escape dismisses the current chart; close links also point at a non-modal fallback fragment. Added the historical Novozymes `NZYM-B:xcse` chart alias to current Novonesis `OMXCOP:NSIS_B`, without changing Saxo symbols or trading behavior.
- 2026-07-23: Added Previous/Next Markov signal navigation beside the table heading as well as at the bottom, so operators can move between bounded server-side pages without scrolling through the current page.
- 2026-07-23: Reworked the Rust Docker build into a dependency-only Cargo layer plus persistent BuildKit Cargo registry/target caches. A tracked build script injects the per-deploy Git SHA only for the final application build, so revision metadata recompiles correctly without invalidating dependencies. Screenshot directories are excluded from the Docker context; a cached rebuild transfers roughly 16 KB of source context and reuses every dependency layer.

- 2026-07-23: Added bounded, read-only Hermes Learning Memory. It promotes repeated safe reflection actions across distinct reflections to `stable`, keeps one-offs as `emerging`, expires emerging lessons after 7 days and stable lessons after 21 days, and keeps stale rows operator-visible but out of Hermes advisory context. It has no mutable memory store, configuration effect, lifecycle action, or Saxo behavior.
- 2026-07-23: Added a read-only Hermes Baseline Promotion Evidence pack. It joins an active baseline to the exact recorded experiment-overlay manager runs, their compact linked execution outcomes, and bounded experiment/post-promotion portfolio observations (return, drawdown, cash use, and sufficient-sample zero-risk-free Sharpe). It is explicitly observational, excludes raw proposal/provider/broker data, and cannot modify lifecycle, configuration, or Saxo behavior.
- 2026-07-23: Added a deterministic, read-only Hermes Proposal Quality Review for active and pending experiments. It scores safe persisted metadata for one-variable purity, attached/named evidence, measurable effect, changed values with risk notes, and exact/related proposal conflicts. It is operator context only and cannot transition a proposal, change configuration, or affect broker behavior.
- 2026-07-23: Added a read-only Hermes One-Variable Audit view. It separates promoted baseline audit artifacts from the exact supported experiment overlay selected by the Trading Manager for its next paper/SIM-eligible cycle, records whether that overlay appeared in the latest manager run, and uses the manager's own selection helper to prevent dashboard/runtime drift. It cannot transition experiments, rewrite configuration, or affect broker behavior.
- 2026-07-23: Added a bounded, read-only Hermes Lessons Pending Review queue. It turns recent reflection `proposed_actions` into deduplicated operator context, redacts sensitive-looking action text, and cannot create/transition experiments, change strategy configuration, or affect broker behavior.
- 2026-07-22: Completed the next Hermes proposal-review slice. In addition to the 14-day stale alert and 30-day safe `expired_stale` closure, the scheduler now sends one sanitized Monday-morning local-time Slack digest per ISO week while proposals remain pending. It reports only safe review metadata and cannot alter proposal lifecycle, strategy, baseline, or broker state.
- 2026-07-22: Routed sanitized Saxo execution taxonomy into Slack failure alerts. Per-order failures and failure-burst alerts now include only the stable category, label, remediation, and retry policy; raw broker diagnostics and local error text remain in the protected execution record and are not sent to Slack.
- 2026-07-21: Hardened the Decision Report dry-run lifecycle. Dry runs now use explicit `dry_run_xai_deferred`, `dry_run_completed`, and `dry_run_error` statuses from provider submission through polling, persist a compact safety boundary in the sanitized report, and are rejected by the Trading Manager status gate. This closes the prior timing gap where a background scheduler could have seen a dry run as an ordinary completed report after the immediate manual path correctly skipped it.
- 2026-07-21: Added a tiered monthly-loss guardrail. While monthly P/L is between the configured `monthly_loss_soft_reduce_dkk` (-25,000 DKK) and hard `monthly_loss_halt_dkk` (-50,000 DKK) floors, the Trading Manager halves the whole-cycle BUY budget through `monthly_loss_soft_buy_multiplier` (0.50); the hard floor still blocks new BUYs and SELLs remain unblocked. The effective budget and tier status are persisted in manager runs, exposed in Cash Deployment, and included in the decision prompt/Hermes preflight. Invalid or disabled floor ordering does not activate the soft tier.
- 2026-07-16: Server-verified flatten-role SELL exits. The technical gate's blind `FLATTEN` escape hatch (which the model's actual `risk_reduction_flatten` role never matched) is replaced by a call-site fallback: flatten-family SELLs blocked on neutral technicals are approved only when the process independently verifies an under-water broker position against a fresh indicator close or a fresh negative Markov regime signal. Covered by pure-gate and database-backed fixtures (under-water, profitable+positive-regime, negative-regime-only, and stale-signal cases).
- 2026-07-15: Added a database-backed SELL final-fill cost-basis fixture. A synthetic four-share Saxo `FinalFill` reads the latest local holding basis, prorates cost basis and commission, books realised P/L, and is idempotent on replay; it performs no Saxo HTTP.
- 2026-07-15: Added a database-backed partial-to-final fill delta fixture. Given a previously persisted one-share partial fill, a later four-share Saxo `FinalFill` books only the remaining three shares and cannot duplicate either fill or ledger records on replay; it performs no Saxo HTTP.
- 2026-07-15: Added a database-backed final-fill reconciliation fixture. A synthetic confirmed Saxo final fill writes one execution fill and one trade-ledger row, backfills the order price, retains the broker payload, and proves a repeated cumulative fill cannot duplicate accounting; it performs no Saxo HTTP.
- 2026-07-15: Added a database-backed broker-sync not-found fixture. When both open-order and audit-activity lookup are absent, local `broker_working` state remains unchanged while the missing visibility and broker order id are persisted as an auditable event.
- 2026-07-14: Added a database-backed broker terminal-state fixture. A synthetic Saxo `Expired` response records a normalized broker event, persists `broker_expired`, preserves the prior local result, and stores broker-sync context without any broker HTTP.
- 2026-07-14: Added a database-backed execution-order claim race fixture using isolated in-memory SQLite. Two concurrent local claim attempts yield exactly one `submitting_to_broker` transition while clearing a stale local error; it never loads a Saxo session or calls broker HTTP.
- 2026-07-14: Added a database-backed Trading Manager queue fixture using isolated in-memory SQLite tables. It verifies idempotent local order/event persistence and manager-run linkage for a scheduled report without a Saxo session, broker HTTP, or a live database.
- 2026-07-14: Added an execution-queue admission regression guard. The pure gate preserves the existing precedence and response shape: only explicit live Saxo execution without dry-run or required approval can reach session loading and broker mutation; all other configurations fail closed before that boundary.
- 2026-07-14: Added always-visible EU and US decision-pulse health to the Operations banner. Each chip reports the latest normalized status and last successful run timestamp; accepted deterministic xAI fallback reports count as successful, matching the Trading Manager's scheduled-report eligibility.
- 2026-07-14: Added the first scheduled-report hand-off regression guard. Trading Manager now accepts only a positive-id completed/fallback report with a scheduled pulse and a parseable timestamp inside its configured freshness window; malformed or undated reports fail closed before they can queue broker work.
- 2026-07-14: Made Operations-banner Markov, Quiver, and Indicators freshness schedule-aware. Weekday-only jobs now render neutral `idle (weekend)` off-hours and `waiting` before their local due time; they warn only when the last successful run precedes the latest configured due date. Current schedule metadata is read from active config, not an old run record.
- 2026-07-14: Made dashboard decision chips age-aware. Portfolio and watchlist labels now use a relative age, explicitly say `Stale` after the configured `strategy.swing.position_decision_stale_after_days` horizon (default seven days), and fail closed to an `Undated` stale state when a source timestamp is absent. This is display-only context; it cannot change a recommendation, queue an order, or affect broker behavior.
- 2026-07-14: Restored scheduler-history retention in the Rust scheduler. Each completed cycle now prunes rows older than the configured age and then applies the configured row cap, matching the legacy policy without blocking the cycle if pruning fails. Physical database space reclamation remains an operator maintenance task.
- 2026-07-14: Added server-side pagination for the Scheduler Cycles table. The Execution tab reads a 12-row bounded projection with a total and Previous/Next navigation instead of loading unbounded `SELECT *` cycle history.
- 2026-07-13: Added server-side pagination for the latest Quiver run's signal table. The tab loads 40 rows per page with a bounded offset and run-scoped total; its success/error pills now use the run’s aggregate metrics rather than the current page.
- 2026-07-13: Added server-side pagination for the latest Markov run's signal table. The tab loads 40 rows per page with a bounded offset and run-scoped total; its success/error pills now use the run’s aggregate metrics rather than the current page.
- 2026-07-13: Added the first paginated dashboard table. Execution now reads 25 orders per selected server-side page, bounds Overview to its 12 most recent queue entries, and bounds other health-strip views to 20 recent orders. Attribution enrichment therefore scales with the displayed page rather than a fixed 80 rows.
- 2026-07-13: Continued the P0 dashboard read-model split. Tab-exclusive collections for Hermes, Execution detail, Markov, Quiver, Watchlists, and End-of-Day now load only when their tab is selected; compact EU/US decision-pulse status remains shared because the Operations banner is persistent across views.
- 2026-07-13: Continued the P0 dashboard payload split. Full portfolio performance history now loads only for the Performance tab and respects its selected range rather than fetching 5,000 rows for every SSR view. The standalone performance API remains unchanged.
- 2026-07-13: Started the P0 dashboard payload split. Routine SSR now loads only compact Decision Report metadata (five rows on Overview, one on other tabs, twenty on the report view); heavyweight prompt/request/response/report JSON is fetched only for the active Decision Reports or AI Prompts detail. The report-history table explicitly marks unloaded counts until a row is selected.
- 2026-07-13: Added deploy provenance verification. Docker now embeds the full committed Git SHA in the Rust binary, `/api/health` exposes it, and `post-deploy-guard` fails unless the running revision contains the requested commit. Image tags alone can no longer validate a safeguard rollout.
- 2026-07-13: Removed database credentials from dashboard-visible runtime metadata. PostgreSQL now displays only host, port, and database name; SQLite displays a generic local label. User-info, connection query parameters, and filesystem paths are excluded, with regression coverage for secret-bearing PostgreSQL URLs.
- 2026-07-13: Added a read-only Decision Report candidate scoring waterfall. It reconstructs the stored Trading Manager preflight and final outcome per candidate, showing compact market/risk, technical, Markov, Hermes quantity effect, normalized gate code, and result. New manager runs persist stable gate codes; historical runs are locally classified but raw Hermes rationale, broker payloads, and raw errors are not rendered.
- 2026-07-13: Added non-mutating Hermes counterfactual tracking. When conservative advice blocks or reduces a candidate, the prevented quantity enters a quote-to-quote shadow ledger, active shadow symbols join the read-only price monitor, and the Hermes tab shows reference/latest price plus estimated directional return. The ledger explicitly excludes broker execution, fees, FX, slippage, taxes, and realised P/L.
- 2026-07-13: Added a normalized Hermes advice delta to every Trading Manager run. Each candidate records matching precedence, requested/resulting quantity, whether advice allowed/reduced/blocked/reviewed/no-oped it, and its final local manager outcome; the delta excludes Hermes rationale, raw broker payloads, and raw execution errors so advisory impact can be measured safely.
- 2026-07-10: Completed the Hermes advice self-check safety gate: Trading Manager advisory prompts require Hermes to state whether it reviewed the latest report, Markov signals, EOD report, positions, and active experiments; stored advice normalizes missing sources; the audit table shows the self-check status; and conservative mode blocks automatic queueing whenever the self-check is incomplete.
- 2026-07-10: Added Hermes's normalized, persisted decision preflight bundle: the exact manager-cycle snapshot now supplies report/candidate waterfall, candidate-relevant exposure, capital and circuit-breaker state, Markov freshness, active experiments, and classified recent failures while excluding Saxo sessions, raw broker payloads, and raw error text.
- 2026-07-10: Added the first decision-action regression guard: manual decision-report actions now share an explicit live/dry-run mode helper, and unit tests prove completed dry-run reports cannot trigger Trading Manager or Saxo execution side effects.
- 2026-07-10: Added an operator acknowledgment/override path for the monthly-loss circuit breaker: the Trading Manager now records threshold breach vs active halt separately, current-month overrides are persisted in runtime settings, and the Overview cash deployment panel can resume or clear BUY resumption for the month with notes.
- 2026-07-10: Added an operator override path for active instrument quarantines: exact symbol/action/signature overrides are persisted in runtime settings, the Trading Manager only bypasses a quarantine when the exact override is active, and the Overview quarantine panel exposes row-level override/clear controls with notes.
- 2026-07-10: Added operator acknowledgments for current Overview integrity mismatches/warnings: each issue receives a stable issue key, acknowledgments are persisted in runtime settings with notes, and the Overview integrity panel can acknowledge or clear current issue acknowledgments without marking the underlying check healthy.
- 2026-07-10: Added broker exposure aggregate reconciliation to Overview integrity: Saxo exposure P/L is compared with dashboard unrealised P/L, exposure quantities are checked against broker position quantities, and drift is surfaced as warning-level integrity rows with stable acknowledgement keys.
- 2026-07-09: Started the price-monitor market-hours-aware polling slice: Saxo session validation now happens before per-poll DB/instrument work, known closed exchanges are skipped before infoprice batching, and extra watch symbols are not resolved while their exchange is closed.
- 2026-07-09: Added persisted price-monitor status and UI visibility for market-hours-aware quote pauses: the Market tab now shows latest quote-monitor status/skipped closed symbols, and the Operations Quotes chip treats closed-market pauses as intentional instead of stale/unknown.
- 2026-07-09: Completed the first price-monitor market-hours-aware polling pass with an explicit `off_hours_poll_interval_minutes` setting; closed-market refresh summaries now slow the scheduler-side quote heartbeat from 1 minute to 15 minutes by default.
- 2026-07-09: Started the Markov coverage cleanup with a persistent Saxo instrument negative cache; definitively unresolvable symbols now skip daily Saxo reference retries for 7 days while stored broker/position instruments still bypass the cache.
- 2026-07-09: Added Saxo share-class symbol variants for Markov, daily indicators, and execution lookup, so symbols like `CARL-B:xcse`, `VOLV-B:xsto`, and `BRK-B:xnys` can match Saxo's compact `CARLb`/`VOLVb`/`BRKb` style without weakening exchange checks.
- 2026-07-09: Added config-driven read-only Markov/daily-indicator symbol aliases for known stale mappings (`COST:xnys`, `HON:xnys`, `LIN:xnys`, `SHELL:xlon`) so chart analytics can recover coverage while persisted rows remain keyed by the original symbol and execution orders are not rewritten.
- 2026-07-09: Added scheduler-driven operational Slack alerts and Hermes dashboard age visibility for stale `pending_review` experiment proposals, so one-variable self-improvement hypotheses page the operator after 14 days instead of silently accumulating.
- 2026-07-10: Added backend duplicate detection for Hermes experiment proposals: exact same `changed_variable_path` now returns `409 Conflict` while an active or pending experiment already covers that variable.
- 2026-07-09: Added scheduler-driven operational Slack alerts for monthly-loss circuit-breaker activation and clearing, based on transitions between latest Trading Manager runs.
- 2026-07-09: Added scheduler-driven operational Slack alerts for newly active derived instrument quarantines, using normalized symbol/action/signature rows without raw broker error payloads.
- 2026-07-09: Added scheduler-driven operational Slack alert routing for overview integrity issues, including high-severity mismatches and overdue DayOrders waiting for broker-sync confirmation.
- 2026-07-09: Added a non-mutating `expiry_pending_broker_sync` lifecycle marker, overview integrity payload, Operations banner health, and Overview Integrity panel for active DayOrders whose exchange-calendar expiry is already past the broker-sync grace window.
- 2026-07-09: Continued the execution-order lifecycle reconciler: Saxo broker sync now records whether state came from the open-order endpoint, audit-activity fallback, or a missing lookup probe, and execution tooltips surface that provenance for stuck `broker_working` orders.
- 2026-07-09: Added execution-order DayOrder lifecycle visibility: active broker orders now carry `order_duration_type`, expected exchange-calendar expiry, market/timezone metadata, and the Overview/Execution tables show an Expiry column with lifecycle tooltip detail.
- 2026-07-09: Surfaced the derived instrument quarantine in the Overview sidebar so active symbol/action blocks, failure signatures, counts, expiry time, and gate config are visible after each Trading Manager run.
- 2026-07-09: Added a derived instrument quarantine in the Trading Manager: repeated identical hard execution failures over the configured lookback window now skip matching symbol/action candidates before queueing, and active quarantines are recorded in manager-run JSON.
- 2026-07-08: Repaired the May 18 import cost-basis corruption end to end: verified all 18 position_snapshots/position_lots rows against the exact old-parser corruption of the original Positioner CSV, restored true values, and recomputed 22 SELL ledger rows via FIFO replay (import lot + subsequent buys). Corrected realised P/L since the reset is +69,251 DKK; per-symbol expectancy analytics are now trustworthy.
- 2026-07-08: Added the monthly-loss circuit breaker: when month P/L breaches `strategy.capital.monthly_loss_halt_dkk` (default -10,000), the Trading Manager skips all new BUYs (SELLs unaffected), records breaker state in manager runs, and the decision prompt tells the model buys are suspended. Verified active on deploy with month P/L -28,277.
- 2026-07-08: Added the commission-efficiency floor for BUYs: `execution.max_commission_pct_per_side` (default 0.3%) turns each exchange's minimum commission into a minimum clip size (XNAS/XNYS ≈ 7,021 DKK, XCSE ≈ 4,667 DKK), enforced in the manager and published per exchange in the decision prompt capital plan.

- 2026-07-08: Replaced the overview `integrity` stub with real invariant checks for portfolio identity, ledger-vs-history cash drift, broker cash drift, implausible position-lot unit cost, and stale/unreconciled execution orders.
- 2026-07-08: Added a database-backed Saxo session refresh lease on the `saxo_sessions` singleton so API, scheduler, and MCP pods do not race Saxo's single-use refresh token during rollouts; auth status, explicit refresh, user logout keepalive, and broker session ensure paths now route through the lease.
- 2026-07-08: Upgraded the FX cache to prefer Saxo FX spot infoprices, falling back to ECB daily rates and then static constants; decision-report indicator context, Markov context, Trading Manager BUY value verification, overview read models, price snapshots, and broker-fill ledger rows now use the cached FX path where async DB access is available.
- 2026-07-07: Added a database-backed `currency_fx_rates` cache fed by ECB daily rates with staleness bounds, static fallback, and cache freshness short-circuiting; price-monitor DKK snapshots and broker-fill ledger rows now use cached FX rates instead of hardcoded active valuation rates.
- 2026-07-06: Added scheduler-cycle duration metrics: each persisted cycle now includes `duration_ms` plus per-step `step_durations`, and the dashboard shows recent cycle runtime in the Scheduler Cycles table.
- 2026-07-04: Added scheduler-driven operational Slack alerts for repeated decision-report failures, execution-failure bursts, stale scheduler completion, and missed Hermes EOD reflection, with runtime notification table creation in Rust.
- 2026-07-04: Exposed operational notification status in the Scheduler Cycles table so alert dispatch health is visible without opening raw cycle JSON.
- 2026-07-04: Added backend-backed decision pulse health rows for Nordic/EU, US, and manual reports, including latest report, last success, last failure, and 7-day attempts.
- 2026-07-04: Added `make post-deploy-guard` backed by non-secret `.run/last_deploy.env` metadata so deploys can verify the cluster is running the expected API, scheduler, MCP, and Hermes images.
- 2026-07-04: Added a CNPG/RustFS backup and restore rehearsal runbook with manual backup checks, RustFS object inspection, safe restore namespace pattern, and verification queries.
- 2026-07-04: Expanded `make post-deploy-smoke` with decision-report schema health validation and optional expected-image checks for API, scheduler, MCP, and Hermes deployments.
- 2026-07-04: Added `make diagnostics-artifact` so read-only diagnostics can be captured to timestamped `.diagnostics/` logs for Slack/GitHub issue sharing without changing the default diagnostic behavior.
- 2026-07-04: Added dependency and CVE hygiene workflow with `make deps-dry-run`, `make security-scan`, RustSec advisory scanning, Trivy filesystem/image CVE scanning, and Trivy secret scanning.
- 2026-07-04: Tightened Docker build-context hygiene so local RustFS object-store data is excluded, then added `make post-deploy-smoke` for read-only rollout, health, overview, scheduler, Saxo-session, MCP tool-discovery, and Hermes gateway checks after deploy.
- 2026-07-02: Added a read-only `make diagnostics` bundle for pod status, rollouts, scheduler/API/Hermes logs, resource usage, CNPG health, RustFS backup state, shared ngrok routing, and sanitized app performance/execution summaries.
- 2026-07-01: Added execution-order attribution linking orders back to decision report, Trading Manager decision, Hermes advice delta, latest daily indicators, and latest Markov state in the Execution view.
- 2026-06-29: Changed Trading Manager SELL sizing to prefer current Saxo broker snapshots over stale imported position snapshots, preventing already-flattened holdings such as ORSTED and NNIT from being queued again.
- 2026-06-27: Added an OpenRouter structured-output schema registry and reusable strict-schema validator so all current response schemas are checked for `additionalProperties: false`, complete `required` lists, union branches, arrays, and stale required entries.
- 2026-06-26: Improved Saxo limit-price handling by using configured overrides, Saxo instrument-details tick schemes, and explicit broker-expired status mapping for unfilled DayOrders.
- 2026-06-25: Added a Decision Report Quality panel that scores completion, schema strictness, normalized section presence, order shape, and market-scope enforcement warnings.
- 2026-06-25: Added an Overview Cash Deployment panel that explains latest Trading Manager reinvestment pressure, buy budget, BUY candidates, approved BUYs, and blocked BUYs.
- 2026-06-25: Added a sanitized decision-report debug panel with expandable prompt, request, provider response, and normalized report payloads plus redaction tests.
- 2026-06-24: Added a dry-run decision report action so provider/schema changes can be tested without Trading Manager or execution side effects.
- 2026-06-24: Added EU/US/manual decision pulse health cards to make missed or failed scheduled reports visible.
- 2026-06-24: Added Execution page broker diagnostics that classify common Saxo failures and render recent order events through the same tooltip/detail formatter.
- 2026-06-24: Added a compact operations banner for Saxo session, scheduler heartbeat, report, Markov, daily indicator, and quote freshness health.
- 2026-06-24: Added a Hermes Decision Advice Audit table that shows whether recent reports received advice, the recommendation, order-advice counts, manager status, queued/executed/failed order counts, and conservative impact.

## Immediate Stabilization

These items reduce operational risk and make the existing system easier to trust.

| Priority | Area | Improvement | Why It Matters |
| --- | --- | --- | --- |
| P0 | Execution safety | Flatten-role SELL fix landed 2026-07-16: `technical_gate` no longer trusts any flatten label (the old exact-`FLATTEN` escape hatch is removed), and a server-verified risk-off fallback at the call site admits flatten-family SELLs (`risk_reduction_flatten` etc.) only when this process independently confirms an under-water broker position against a fresh verified close, or a fresh negative Markov regime signal. Follow-up: observe the next defensive exits actually queue. | Live evidence 2026-07-15/16: reports 173 and 175 suggested `SELL ARM:xnas 5` as `risk_reduction_flatten` while ARM sat at −4,221 DKK unrealised; both were skipped with "SELL not approved; technical sentiment is HOLD with neutral trend" in a week where the portfolio bled −24.4k DKK. The old gate blocked exactly the defensive exits the capital-preservation plan calls for. |
| P0 | Decision prompt integrity | Landed 2026-07-16/17: `watchlists_payload` entries older than `position_decision_stale_after_days` (7d) are stripped to a bare row so months-old prices, sentiment, and "Existing portfolio holding" rationales cannot reach prompts as current data. Landed 2026-07-21: the 197-symbol analysis universe is now versioned in `market_data.watchlists.universe_symbols` in both runtime configs; current broker positions and explicit extra watches remain additive, symbols are case-normalized, and archive membership is a warning-level fallback only when configuration is missing. Follow-up: observe the next nightly run and prompt for coverage stability. | 5 of the week's 11 Trading Manager skips were phantom NNIT/ORSTED SELLs (reports 166/168/170/172/174) — the broker-authoritative guard correctly blocked them, but the model kept burning suggestion slots on positions that did not exist because the prompt told it they did. |
| P1 | Accounting integrity | Broker-basis fallback landed 2026-07-16: `latest_position_cost_basis` now falls back to the broker-authoritative open price (including costs, FX-converted) whenever the local snapshot is missing or carries no usable basis, and warns loudly if neither source exists — a SELL of an outside-ledger position can no longer book its full proceeds as realised gain. The broker-bootstrap SQL (`scripts/broker_bootstrap_20260716.sql`) was applied by the operator 2026-07-16 and verified: 13 live positions carry snapshot+lot rows (batch `broker-bootstrap-20260716T190000Z`), the 18 stale 2026-05-18 rows are excluded, and unit costs stay under the 100k integrity threshold. Fill-time book keeping landed 2026-07-17: every reconciled fill now maintains the local book — BUY fills upsert the `position_snapshots` row (quantity plus commission-inclusive basis, in place) and insert a `position_lots` row (`source_type = 'buy_fill'`, idempotent by lot id), SELL fills remove quantity and prorated basis — so future positions never depend on the broker fallback and proration stays correct across partial exits and re-buys. Divergence alert landed 2026-07-17: every broker read-model refresh (10-minute cycle) compares latest local snapshot quantities against broker open quantities in both directions and logs a structured warning plus a `quantity_divergences` result field on mismatch. Remaining work: FIFO `lot_realizations` at SELL time if per-lot tax accounting is ever needed. | Every position acquired after 2026-05-18 (in-ledger DANSKE/CHEMM/AMGN buys included — BUY fills never wrote snapshots) had NO local basis, so any SELL booked cost basis 0 — the exact failure class repaired on 2026-07-08, waiting to fire on the first flatten exit. |
| P1 | Signal coverage | Landed 2026-07-16: `strategy.swing.daily_indicators.max_symbols` raised from 20 to 0 (unlimited), matching the Markov run's full portfolio+watchlist universe (~199 symbols). The decision prompt still receives only the top 80 signals by confluence count, while the Trading Manager gate reads per-symbol signals directly, so every candidate BUY now has verifiable technicals. Follow-up: confirm tonight's 23:45 run covers the full universe and watch chart-API pacing. | Nightly runs covered exactly 20 assets (holdings plus legacy symbols), so watchlist candidates like DSV:xcse had no daily technicals and every candidate BUY was stood down with "0/3 confluences" (reports 170 and 174) — the system could not rotate into new names while the book decayed. |
| P2 | Risk governance | Landed 2026-07-21: documented and tiered the monthly-loss policy. Rescaled 2026-07-25 against the +15%/year goal: the soft floor is now `-9,000 DKK` (-3% of a ~300,000 DKK book) reducing the cycle-wide BUY budget to 50%, and the hard halt is `-18,000 DKK` (-6%). The previous -25,000/-50,000 pair let a single month erase roughly a year of target gains before the halt fired. The soft floor must be negative and less severe than the hard floor or it is disabled. Follow-up: observe the new tiers in live manager runs, and express both floors as a share of portfolio value rather than fixed DKK. | Replaces a binary response with a visible, deterministic reduction while preserving the hard capital stop and unrestricted defensive SELLs. |
| P1 | Execution provenance | Landed 2026-07-25. `CandidateOrder::from_json` read `strategy_type` from the model's suggested-trade JSON, but the decision-report schema has no such field, so it was NULL on all 101 orders the Rust runtime had queued (most recent 2026-07-23) while the legacy Python rows through 2026-05-07 carried `swing`. The runtime now sets `TRADING_MANAGER_STRATEGY_TYPE` (`swing`) at insert and ignores any value the model supplies, a startup backfill scoped to `report_id IS NOT NULL` repaired the 101 historical rows, and the UI fallback is `unknown` rather than `manual`. See [urgent-todo](urgent-todo.md) U8. | The Execution table showed every automated order as `manual`, inverting the most important fact about an order's provenance, and Slack labelled them `Execution` rather than `Trading Manager`. The "attribution by decision pulse" item below is now buildable on this column. The general lesson: provenance must be recorded by the component that knows it, never requested from the model. |
| P0 | Risk configuration integrity | Audit landed 2026-07-25 in `src/config_contract.rs`: every `strategy.*`, `risk.*`, and `taxation.*` key is classified enforced/advisory/unused, startup warns on each unused risk key, the Overview integrity payload carries the list, and a key added to config without a contract entry raises a warning. The 2026-07-26 remediations made the strategy, Trading Manager, EU/US scheduled-pulse, `RISK_EXCLUDED_SYMBOLS`, Danish share-income estimate, ATR-based risk-per-trade, deterministic BUY cost controls, per-report candidate ceiling, ladder per-symbol exposure cap, maximum-holdings cap, post-gate BUY-selection cap, and duplicate cash-buffer retirement real. Against the shipped configuration it now reports 34 enforced, 30 advisory, 29 unused — 12 of them risk-surface. Follow-up: each of the 12 needs an operator decision to implement or delete, starting with concentration and the four separate unenforced position-weight caps. See [urgent-todo](urgent-todo.md) U2 for the full list. | This is the retired `cash_buffer` failure class, except undetectable by inspection: an unwired key reads exactly like an enforced one. No concentration gate exists (the string `sector` is absent from `src/`), and the remaining position/breadth controls still need an explicit operator decision. |
| P1 | Risk governance | Landed 2026-07-26: a trailing portfolio drawdown guardrail now shares the monthly-loss tier mechanism. It reduces the cycle-wide BUY budget in its soft band and suspends new BUYs at its halt floor; SELLs remain unblocked. The guardrail persists sanitized evidence in manager runs, is visible in Cash Deployment, and its halt level is the same value published to Hermes as `max_drawdown`. | The goal contract and runtime now share one config source, so Hermes cannot reason about a drawdown limit the manager does not defend. Insufficient history is explicit rather than silently treated as a safe book. See [urgent-todo](urgent-todo.md) U3. |
| P0 | Security | Landed 2026-07-25: screen and delimit third-party editorial text before it reaches Decision Reports or Hermes. A narrow deterministic marker list flags model-directed text; flagged rows remain available for operator review but are excluded at the compact-context boundary, including rows stored before screening existed. Prompts explicitly label remaining editorial material as untrusted. See [urgent-todo](urgent-todo.md) U4. | Market language such as “upgrade to Buy”, “sell off”, or “disregard one month of data” remains usable, while instruction-shaped text cannot occupy scarce model context. The deterministic Trading Manager gates remain independent of editorial text. Future feed expansion must preserve this boundary and add measured usefulness rather than widening trust. |
| P0 | Security | Redact the database connection string in the dashboard Runtime panel. | Landed 2026-07-13: `DashboardView.db_label` now uses a structured, display-only label that excludes credentials, query parameters, and local paths; the known dashboard and startup-log display paths were audited. |
| P0 | UI performance | Continue splitting dashboard reads per tab. | Decision Report lists exclude prompt/request/response/report JSON except for active detail; portfolio history loads only on Performance; tab-exclusive collections load only on their own tabs; Execution orders, Markov signals, Quiver signals, and Scheduler Cycles are server-paginated with bounded shared views. AI Prompts now loads server-sanitized debug payloads only after an operator expands them. Remaining work: paginate any newly introduced long shared tables and add similarly bounded detail endpoints. |
| P0 | Deploy provenance | Verify the binary’s Git revision after every Kubernetes rollout. | Landed 2026-07-13: Docker embeds a full commit SHA, `/api/health` returns it, and `post-deploy-guard` requires the running revision to be equal to or descend from the requested commit, closing the matching-tag/stale-image gap observed on 2026-07-09. |
| P1 | Data hygiene | Landed 2026-07-22: remove the retired Python-only `strategy.capital.cash_buffer` override during Rust startup. The cleanup is idempotent and preserves active Rust settings (`ai_api_key`, `ai_model`, and `manual_report_claim`). Follow-up: only add expiry/provenance metadata if a new non-secret trading runtime-override surface is deliberately introduced. | The 2026-05-05 zero-buffer override can no longer be resurrected by a future compatibility refactor. |
| P1 | Data hygiene | Scheduler cycle retention landed 2026-07-14: each Rust scheduler cycle prunes rows older than `history_retention_days` and then enforces `history_max_rows` (250/30d in Kubernetes), matching the legacy policy. `audit_log` dead space landed 2026-08-03: the table (65 MB, 38% of the database, dead since the Python cutover) was dropped after operator confirmation, once a real prerequisite was caught and fixed first — the U13 autovacuum-tuning list still referenced it and would have crash-looped every future pod on the missing table otherwise. Database 172 MB → 109 MB. Remaining dead-tuple counts across other tables are small (low tens of rows, not a bloat problem) and autovacuum-managed under U13's tightened settings; no further maintenance plan is needed unless growth changes that picture. | Cycle JSON at ~5.5 KB/row grew unbounded and slowed scheduler-history queries; the retention policy now bounds it, and `audit_log`'s physical dead space no longer inflates backups or restore rehearsals. |
| P0 | Decision reports | Keep the OpenRouter structured-output schema registry current whenever new strict schemas are added, including Hermes prompts if they use strict schemas later. | Prevents repeat `invalid_json_schema` outages. |
| P0 | Execution safety | Landed 2026-07-21: persist a sanitized execution failure stage (`request_build`, `precheck`, `placement`, etc.) beside the existing error taxonomy. Execution Queue order and event rows now show the stage and include it in tooltips, diagnostics, and remediation context. Follow-up: observe new broker failures for an unclassified stage before adding more categories. | Distinguishes local validation, Saxo precheck, and Saxo placement failures without exposing credentials or raw broker payloads. |
| P0 | Scheduler | Per-pulse scheduled report health landed 2026-07-14: the persistent Operations banner shows EU/US latest status plus last successful run. | Makes missed EU/US pulses visible immediately. |
| P1 | Risk guardrail | Continue observing the monthly-loss circuit breaker override path added 2026-07-10; next follow-up is an audit/history view if overrides become common. Slack alerting for activation/deactivation landed 2026-07-09. | The breaker now has a month-scoped operator resumption path, but repeated overrides should be visible as risk governance evidence. |
| P1 | Accounting integrity | Continue observing the Overview integrity system after UI surfacing, Slack routing, operator acknowledgments, and broker exposure aggregate reconciliation landed. Broker-cash reconciliation is now explicitly opt-in because the deployed Saxo SIM account is an independent EUR capital book while the application manages a bounded DKK strategy ledger. Follow-up only if runtime evidence shows additional same-book invariants are needed. | The May position_lots corruption (3.2M DKK cost basis on 31 shares) sat unnoticed for a month; invariants with alerts catch the next one in hours. |
| P1 | Markov coverage | Continue fixing the assets that fail instrument resolution on daily Markov runs: negative caching, share-class compact symbol variants, and read-only configured aliases landed 2026-07-09; remaining work is verifying the next daily run and correcting any genuinely unresolved exchange mappings. | 19% of the universe was missing regime signal coverage. Weekly negative caching stops repeated dead-end lookups, share-class variants recover many `*-A`/`*-B` Nordic and Berkshire-style rows, and explicit aliases recover known stale symbols without weakening exchange checks or rewriting orders. |
| P1 | Instrument quarantine | Continue observing the derived quarantine override path added 2026-07-10; follow-up only if override history needs a separate audit table or if derived signatures become too expensive to compute. Slack alerting for active derived quarantines landed 2026-07-09. | Known-broken instruments waste daily order capacity, pollute execution stats, and re-teach the model nothing; overrides now require exact symbol/action/signature and notes. |
| P1 | Price monitor | First market-hours-aware polling pass landed 2026-07-09: session-first checks, closed-exchange skips, persisted UI visibility, and explicit slow off-hours heartbeat. Follow-up only if runtime evidence shows per-exchange cadence needs finer tuning. | Cuts ~two-thirds of infoprice calls, stops `updated_at` implying freshness for closed markets, and reduces log churn during reauth gaps. |
| P1 | Testing | Continue expanding workflow tests. Decision action, scheduled-report freshness, Hermes conservative self-check, execution-queue admission, database-backed Trading Manager queue persistence, concurrent execution-order claim, broker terminal-state, broker-sync not-found, BUY final-fill reconciliation, BUY and SELL partial-to-final delta reconciliation, and SELL final-fill cost-basis guards have landed. Follow-up only when reconciliation behavior changes or live evidence reveals another fill-state transition. | Protects the most critical cross-module workflows. |

## High-Leverage Workflow Improvements

These are specific changes that could improve the quality of trading decisions while keeping the system safe and measurable.

### Trading Quality

| Idea | Shape | Measurement |
| --- | --- | --- |
| Trade thesis ledger | Landed 2026-07-26: every BUY admitted by the Trading Manager records a compact, immutable thesis snapshot containing report/pulse provenance, intended two-week holding window, entry rationale, catalyst/monitor, approval evidence, technical and Markov evidence, and a review-only invalidation condition. Execution now aggregates the newest 50 recorded BUY theses against reconciled fill and later stored-close evidence, showing 1-session/5-session directional returns only after collection begins and keeping the result `collecting` until 20 mature 5-session samples exist. It never authorizes a trade or automatic exit. Follow-up: add an equally bounded eventual-exit/accounting comparison after mature history exists. | Compare realized outcome to thesis after 1 day, 5 days, and exit. |
| Candidate scoring waterfall | Landed 2026-07-13; clarified 2026-07-21: selected Decision Reports show the stored manager preflight and outcome as a compact sanitized waterfall, including market/risk, technical, Markov, Hermes quantity effect, result, and stable gate code. New manager outcomes also persist a compact final, database-verified technical snapshot; the UI renders it with the preflight value and explains BUY/SELL technical rules, so an overridden technical signal is not mistaken for the final gate evidence. Follow-up only if replay data shows a missing deterministic gate needs to be added. | Show why candidates were accepted or rejected and which gate blocks most trades. |
| Post-trade attribution | Execution order details attach each order to its report, Trading Manager decision, Hermes advice, report-time technical/Markov/capital snapshots, reconciled fill-ledger outcome, and its latest prior BUY thesis. Landed 2026-07-26: reconciled fills carry read-only 1-session/5-session directional price outcomes from persisted daily closes, an observed cross-order fill lifecycle (`entry`, `add`, `reduce`, `exit`, or explicit `partial_history`), and a bounded thesis outcome aggregate. The aggregate is `collecting` until 20 mature five-session observations; it is never realised P/L or broker position truth and excludes FX, commissions, tax, slippage, later position changes, outside-ledger inventory, broker adjustments, and causal claims. Remaining work: bounded eventual-exit/accounting evidence; the recorded invalidation remains a review condition and does not automate exits. | Detect which pulses and filters actually add value. |
| Missed-trade shadow book | Landed 2026-07-27: the Hermes-only counterfactual ledger is now complemented by a separate, bounded manager-gate shadow book. It records selected candidate blocks caused by timing, capital, capacity, or active loss guardrails with only the compact gate code, quantity, reference price, and quote-to-quote observation. Technical/Markov validity failures, exclusions, and instrument quarantines are deliberately excluded. The Hermes view now aggregates observed rows overall and by stable gate code, equal-weighted across instruments and marked `collecting` until 20 observations. It never reopens a gate, makes a Saxo/provider call, or claims a blocked trade should have been placed; P/L is not combined across currencies. Follow-up: review only once a preliminary sample exists, then decide whether a one-variable experiment is justified. | Measure whether blocks avoided losses or missed gains. |
| Cross-cycle BUY reservations | Landed 2026-08-19: the Trading Manager now loads active BUYs in its current execution mode/adapter before each cycle and reserves their remaining DKK value in both cash and deployment headroom, while adding their symbols to holding, exchange, currency, and position-weight exposure. It subtracts recorded partial-fill quantity, retains ambiguous broker states until a terminal outcome, and blocks fresh BUYs if an active reservation lacks a reliable DKK value. | Prevent a later Decision Report from spending cash or symbol headroom already committed by an earlier queued or broker-working BUY. |
| Alt-data conflict surfacing | Landed 2026-07-27 for current holdings: strong bearish Quiver Congress-trading signals (`<= -0.35`) against a positive-quantity holding are surfaced as compact review flags in the Decision Report prompt, Hermes context, and Quiver view. The flags neither create an exit nor affect any manager gate. Follow-up: record and evaluate how model-proposed BUYs align with Quiver only after an explicit, bounded candidate-level evidence design. | Quiver stays advisory; the current slice makes disagreement visible without converting alternative data into a trading rule. |
| Adaptive cash deployment | Replace fixed "cash above buffer means buy" with tiers based on volatility, drawdown, signal quality, and number of independent qualifying assets. | Improve cash utilization without forcing weak trades. |
| Position lifecycle state | Track whether each holding is starter, add candidate, hold, trim, exit candidate, or blocked. | Prevent random re-entry/rebuy behavior and make position sizing intentional. |
| Cooldown and churn guard | Add symbol-level cooldowns after failed orders, recent exits, or conflicting reports. **The "recent exits" trigger specifically was investigated 2026-08-03 and found not evidenced**: of every fast BUY→SELL round trip in the trading history, only one was a genuine discretionary reversal (see Trading Quality section above); the rest were protective stops, a one-time launch bootstrap, or manual orders. A cooldown keyed on holding period alone would have penalized two correctly-functioning stop-outs. The "failed orders" and "conflicting reports" triggers are separate ideas and untouched by this finding. | Reduce repeated attempts in noisy names and failed instruments. |
| Gate replay harness | Landed 2026-07-23: Decision Reports and `GET /api/decision/gate-replay` now run bounded, read-only historical target-gate comparisons over sanitized persisted manager snapshots. The initial one-variable scenarios test a fresh-long Markov starter threshold of `0.25` and BUY technical confluence threshold of `4`; results identify target-gate flips without claiming full approval. No provider/Saxo call, order creation, or configuration mutation is possible. Follow-up: operator-selected hypothetical values and experiment-proposal prefill once enough retained evidence exists. | Calibrate gate thresholds from evidence instead of intuition: show exactly which historical approvals/blocks would flip under a proposed setting before promoting it as an experiment. |
| Unified tuning evidence dashboard | First through seventeenth slices landed 2026-08-21: typed, read-only, lazily loaded Tuning tab compares the four scheduled pulses across a 30-day window, with report success and explicit shadow candidate/reference/1-5-20-session/estimated-after-cost denominators. Shadow lanes report server-normalized change assessments, saved decision-time Support/Risk, Markov, and Quiver coverage/buckets, signal-gate source/result, and record-only Hermes evidence. The execution lane exposes persisted local lifecycle-status coverage (queued, broker-active, unknown, and terminal states), current protective-stop coverage, report-to-manager candidate counts for EU/US opening pulses, and newest-recorded BUY thesis outcomes without polling Saxo or inferring latency/current broker state. Thesis evidence carries its own newest-50 scope, maturity threshold, and gross-only label rather than being presented as a 30-day pulse metric or realised P/L. A separate experiment-governance lane presents retained lifecycle status counts only: it exposes neither proposal values nor performance claims, and cannot activate an experiment. A separate one-month account-value outcome lane reports local snapshot confidence/freshness, latest DKK value including cash, simple change/return, and max drawdown; it is explicitly not realised P/L, time-weighted or total return, and has no cash-flow/FX/dividend/fee/tax normalization claim. The separate one-month benchmark lane compares that local account value with stored native-currency ETF proxy price returns, retaining per-reference status/alignment/freshness and the collector's stored run status/time/date and success/error/reference coverage. These are explicit observational contexts—not forecasts, Trading Manager decisions, broker results, or substitutes for missing data—and remain separate from the execution lane's local order/fill/ledger/daily-close evidence. Markov and Quiver are never refreshed or rerun in the tuning view; the outcome lane reads local history/current aggregate and the benchmark lane reads stored closes/run records without a refresh. Maturity/as-of/safety remain visible; no composite score. Follow-up: add benchmark attribution only with its own range, currency, cash-flow, FX, dividend, and total-return labels. See [shadow mid-session Decision Reports and tuning evidence](decisions/2026-08-19-shadow-mid-session-decision-reports.md). |
| Calendar-month goal context in Tuning | Landed 2026-08-21 (eighteenth slice): typed, read-only monthly progress against the configured DKK planning target, using the active import batch's calendar-month account-value baseline. It preserves target, change, target ratio, baseline, period start, and pending-baseline status, while explicitly excluding realised P/L, time-weighted/total return, and cash-flow/FX/dividend/fee/tax normalization claims. It cannot influence sizing, risk, Hermes, a manager gate, queue, broker precheck, or Saxo execution. | Keep progress toward the portfolio-growth objective visible without converting a static planning figure into a trading target or performance attribution. |

### Hermes Advisory Loop

| Idea | Shape | Measurement |
| --- | --- | --- |
| Unstick the experiment review queue | Pending-review aging alerts through Slack/dashboard landed 2026-07-09; exact same-variable duplicate rejection landed 2026-07-10. Landed 2026-07-22: the scheduler auto-closes only unreviewed proposals beyond the explicit 30-day default as `expired_stale`, with an auditable `scheduler` actor and no activation side effects; a once-per-local-ISO-week Monday review digest lists safe pending metadata before closure. Remaining work: near-duplicate semantic merging before insert. | Median time from proposal to decision; zero proposals older than the configured expiry window; the self-improvement loop actually completes its cycle instead of accumulating unreviewed hypotheses. |
| Hermes preflight contract | Landed 2026-07-10: Hermes receives a compact normalized manager-cycle bundle with report summary, candidate waterfall, candidate-relevant exposure, Markov freshness, active experiments, and classified recent failures. The exact snapshot is retained in `trading_manager_runs.manager_json` for audit/replay; sensitive session, broker-payload, and raw-error data is excluded. | Higher advice consistency and fewer irrelevant comments. |
| Advice delta audit | Landed 2026-07-13: each manager run records the normalized per-candidate applied advisory effect, quantity delta, matching precedence, and final manager outcome without raw rationale/broker data. | Quantify Hermes impact separately from model/report impact. |
| Counterfactual tracking | Landed 2026-07-13: conservative advice blocks/reductions now create a non-mutating quote-to-quote shadow ledger for only the prevented quantity. Active rows enter the read-only price monitor and the Hermes tab displays reference/latest quote, estimated directional return, and status. Fees, FX, slippage, tax, broker execution, and realised P/L remain excluded. | Compare Hermes restrictions against transparent directional shadow outcomes without treating them as realised performance. |
| Proposal quality rubric | Landed 2026-07-23: deterministic, read-only score for active/pending proposal metadata: one-variable purity, evidence presence/source names, measurable effect, changed values/risk notes, and exact/related duplicate risk. `review_ready` is advisory only; other states surface evidence or duplicate review work. | Promote fewer vague proposals and more testable experiments. |
| Learning memory compression | Landed 2026-07-23: bounded read-only reflection-action projection. Repeated actions across two distinct reflections become `stable`; recent one-offs are `emerging`; emerging/stable lessons expire after 7/21 days. Stale rows remain auditable in the UI but are excluded from Hermes advisory context. | Prevent old lessons from overweighting new market conditions. |
| Hermes self-check | Landed 2026-07-10: before writing advice, Hermes must submit a structured context self-check for latest report, Markov run, EOD report, current positions, and active experiments. The audit table surfaces missing context; conservative mode fail-closes automatic queueing when any required source is missing, even if per-order advice says `allow`. | Detect missing context before advice is trusted. |

### Decision Report Flow

| Idea | Shape | Measurement |
| --- | --- | --- |
| Dry-run first-class mode | Add `generate_report?mode=dry_run` that never queues or executes orders. | Safer model, schema, and prompt experiments. |
| Two-stage report | Stage 1 selects/scans candidates. Stage 2 produces trade orders only for candidates that pass deterministic gates. | Less model freedom in order construction. |
| Deterministic pre-report context | Persist the exact context snapshot used for each report: portfolio, candidates, market status, Markov, daily indicators, cash budget. | Reproducible reports and easier debugging. |
| Strict normalized output | Keep the model schema small and deterministic, then enrich locally with calculated fields. | Lower provider/schema failure rate and less hallucinated math. |
| Report confidence and quality score | Calculate parse validity, data freshness, market scope consistency, budget consistency, and number of unsupported claims. | Block low-quality reports before Trading Manager. |
| Provider/model A/B harness | Run OpenRouter models side-by-side in dry-run mode over the same context. | Compare usefulness, parse reliability, cost, and latency before switching production. |
| Prompt regression corpus | Phase 1 landed 2026-07-26: source-controlled schema-valid provider fixtures exercise parser variants, EU scope enforcement, completion normalization, and dry-run safety without a provider call. Follow-up: add anonymized real incident fixtures whenever a provider/parser regression is diagnosed. | Catch prompt/schema regressions without waiting for market hours. |
| Scheduled shadow mid-session reports | Planned 2026-08-19: retain the two execution-eligible open +75-minute reports and add server-enforced shadow pulses at 14:15 Europe/Copenhagen for configured European exchanges and 14:15 America/New_York for XNAS/XNYS. The US pulse normally displays as 20:15 Copenhagen and automatically becomes 19:15 during the short US/EU DST mismatch. Keep it explicitly regular-session-only even if Nasdaq begins its expected 23/5 schedule in December 2026: persist a typed session identity, model the 20:00-21:00 ET pause, and never infer Saxo/account/instrument extended-hours eligibility from venue hours. Saxo exchange-calendar/current-session checks suppress holidays and abnormal sessions. Shadow metadata is server-owned; reports may persist context, pure gate evaluation, record-only Hermes advice, and priced outcomes but can never insert `execution_orders` or reach Saxo precheck/mutation. Observe at least 20 eligible days and 20 mature five-session candidates per pulse before a separate promotion decision. See [decision record](decisions/2026-08-19-shadow-mid-session-decision-reports.md). | Measure candidate novelty, no-new-information rate, 1/5/20-session after-cost outcome, adverse/favourable excursion, implied turnover, and marginal value versus the same-market opening pulse and doing nothing. Zero shadow queue rows or Saxo mutations is a hard invariant. |

### Saxo API And Broker Integration

| Idea | Shape | Measurement |
| --- | --- | --- |
| Instrument capability cache | Cache resolved instrument metadata: UIC, asset type, currency, exchange, tradability, supported order types, tick size, commission/precheck blockers. | Reduce repeated Saxo resolve/precheck failures. |
| Precheck-only probe job | Periodically precheck watchlist instruments with tiny/safe hypothetical orders where allowed, without placement. | Identify commission/tradability blockers before reports recommend them. |
| Market-hours validator | Use Saxo exchange calendars and instrument market state together before queueing orders. | Avoid orders in closed or ambiguous sessions. |
| Order lifecycle reconciler | Reconcile local orders with Saxo open orders, audit activities, fills, expiries, and cancellations every scheduler cycle. Visibility slices landed 2026-07-09: active DayOrders now show expected exchange-calendar expiry, broker-sync provenance records open-order vs audit-activity vs missing-lookup state, and overdue active DayOrders are flagged in row tooltips, overview integrity, the Operations banner, the Overview Integrity panel, and scheduler-driven Slack alerts as expiry-sync-pending without mutating broker state. | Fewer stuck `broker_working` or stale local statuses. |
| Tick and currency normalizer | Centralize price rounding, display currency, order currency, and estimated DKK conversion. | Prevent DKK/USD display mistakes and tick-size rejections. |
| Session health preflight | Before reports and queue processing, assert access token, refresh token, account key, and environment are valid. | Avoid false trade failures caused by reauth drift. |
| Saxo error taxonomy | Landed 2026-07-21/22: every newly recorded local Saxo execution failure persists an identity-safe taxonomy object (stable code, label, remediation, retry policy) alongside the raw diagnostic. It covers broker-state uncertainty, session expiry, rate limits, commissions, cash, tick/price, quantity/holdings, instrument, market, terminal broker status, and unknown. Execution tooltips, Hermes preflight, per-order Slack failure alerts, and burst alerts consume only allow-listed taxonomy fields; raw broker diagnostics and local error text remain outside Slack. Follow-up: add documented Saxo error-code mappings as live evidence accumulates. | Better UI, operations, and Hermes learning from execution failures without exposing raw broker error text. |

### Saxo OpenAPI Capabilities To Adopt (reviewed 2026-07-11)

The runtime currently uses a small slice of the OpenAPI: `/port/v1` snapshots, `/chart/v3` history, `/trade/v1/infoprices` polling, `/trade/v2` orders + precheck, `/ref/v1` instruments/exchanges. The reference docs offer several capabilities that map directly onto existing polling pain. Streaming is verified reachable on SIM (`wss://sim-streaming.saxobank.com/sim/oapi/streaming/ws/connect`, plain WebSocket, same OAuth token, ContextId + per-subscription ReferenceId, delta messages over a stored snapshot, resumable via `?messageid=` and re-auth via `PUT /streaming/ws/authorize`).

| Idea | Shape | Measurement |
| --- | --- | --- |
| Streaming price subscriptions | Replace the 1-minute infoprices polling loop with `POST /trade/v1/infoprices/subscriptions` bound to one WebSocket stream; keep the poller as automatic fallback when the socket drops. One stream carries all held + extra watch symbols. | Quote latency from ~60 s to seconds; infoprice REST calls near zero during market hours; price monitor becomes push-driven with a heartbeat check. |
| ENS activities subscription for fills | Subscribe via `/ens/v1/activities/subscriptions` (activity types: Orders, Positions, MarginCalls, AccountFundings) on the same WebSocket instead of fast-polling broker order sync every minute while orders are open. ENS is streaming-only (no REST snapshot); recovery uses `FromDateTime`/`SequenceId` replay — events are retained 3 days on the streaming replay and 14 days via `GET /ens/v1/activities` (replay capped at 50 msg/s). Persist the last processed SequenceId so reconnects and pod restarts resume gap-free. | Fill-to-ledger latency from ~1 minute to seconds; scheduler fast-poll mode becomes a fallback; no more missed `broker_expired` transitions between cycles. |
| ENS 14-day activities backfill | Phase 1 landed 2026-07-26: once per UTC day the scheduler performs a bounded, read-only `GET /ens/v1/activities` over the preceding 14 days for Order and Position activities. It stores only sanitized aggregate coverage, local broker-order match counts, and capped-page state in `runtime_settings`; no raw broker event, symbol, account, identity, order status, fill, ledger, or broker mutation is written. **Boundary bug fixed 2026-08-03:** the lookback computed `now - 14 days` exactly, which sits on Saxo's exclusive limit (`ModelState.FromDateTime: "Maximum 14-days old activities can be fetched."`) and fails a 400 the instant the wall clock passes the original request's time-of-day on the 14th day — latent since the feature landed, surfaced in a routine diagnostics pass, reproduced live before fixing, now a 13-day window with a pinned regression test. Follow-up: safely page the response, define event-level matching against `execution_orders`/`execution_fills`/`trade_ledger`, and alert on confirmed divergence. | Establishes a broker-authored recovery signal without letting a new feed alter trading state. Full audit reconciliation remains deliberately unclaimed until event matching is deterministic and paginated. |
| Unknown-state order timeout handling | Phase 2 landed 2026-07-21: each broker-sync cycle now uses a read-only bounded `cs/v1/audit/orderactivities` lookup for `broker_state_unknown` orders, matching the retained `ExternalReference` exactly. A confirmed activity records the Saxo `OrderId`, moves the local order to `submitted_to_broker`, and hands it back to normal state sync; no match stays blocked and retains SELL reservation. The audit payload is identity-redacted and neither branch replays placement. Follow-up: add ENS replay or paginated audit-history coverage for activity windows too busy for the bounded response. | Eliminates the double-order failure mode and makes a confirmed broker outcome recoverable without manual inspection, while absence remains fail-closed. |
| Tradable Prices for limit anchoring | The Trade service distinguishes lightweight `InfoPrices` (display) from `Prices` (tradable quotes with quote ids and commission detail, streamable). The delayed-price limit-order logic currently anchors offsets on infoprices; anchoring on a tradable price request just before placement would cut the "limit too far from market" rejections and expired DayOrders. Also expose `/trade/v1/messages` (trade confirmations, margin calls, price alerts) into the ops alert path. | Fewer `broker_expired` limit orders; broker messages surface in Slack instead of only inside Saxo's UI. |
| Broker-computed realized P/L cross-check | Pull `/port/v1/closedpositions` (FieldGroups include closed P/L and open/close prices) and reconcile against locally computed FIFO realized gains per symbol/period as an accounting invariant with Slack alerting on divergence. **Verified reachable 2026-08-02: returns 200 in SIM with 6 closed positions.** Promoted in priority by [urgent-todo](urgent-todo.md) U10 — the local ledger was found reporting FX attribution as a hardcoded zero, which is exactly the class of arithmetic error this cross-check exists to catch. | The 2026-07-08 cost-basis repair would have been caught within a day by a closed-position divergence alert instead of a month later. |
| Corporate actions | **Not covered anywhere in the runtime.** No handling exists for splits, reverse splits, dividends, spin-offs, or mergers. A split silently multiplies share count without adjusting cost basis, which presents as a large unexplained loss and would be indistinguishable from a genuine drawdown by the guardrail that now halts buying. `/ca/v2/events` returns **403 in SIM** (2026-08-02), so the entitlement question has to be settled before design. The 2026-08-02 universe audit found one live instance of the general problem: `NZYM-B:xcse` is unresolvable because Novozymes merged into Novonesis. | Cost basis survives a corporate action; a merged or split holding is detected rather than silently mispriced. |
| Trading schedule per instrument | `/ref/v1/instruments/tradingschedule/{Uic}/{AssetType}` gives authoritative per-instrument session hours including half-days and holidays. Market-open decisions currently derive from `/ref/v1/exchanges` at exchange granularity, which cannot express an early close. | Fewer orders placed into a market that is closed or about to close; the protective-stop sweep's market-open skip becomes precise. |
| Live FX rates via FX-spot infoprices | Implement the live-FX roadmap row concretely: subscribe (or poll hourly off-market) to FX spot infoprices for USDDKK/EURDKK/GBPDKK/SEKDKK/NOKDKK Uics and persist into an fx_rates table consumed by `fx_rate_to_dkk`, with the static table as bootstrap fallback. | DKK valuations track real FX; ledger rows store the actual rate at fill time. |
| Account performance history | Use the `hist` service's performance/timeseries endpoints for broker-computed account value and time-weighted returns, rendered next to the local `portfolio_value_history` series. **Verified 2026-08-02: `/hist/v4/performance/timeseries` returns 200 with an account-value series back to 2021; `/hist/v3/perf` is 404, so v3 references are stale.** This matters more than it did when first written — `portfolio_value_history` is now the input to a guardrail that suspends buying, so its peak is load-bearing and currently unvalidated against any external source. | Independent verification of the Performance tab; discrepancies surface data bugs (frozen daily P/L, stale prices) automatically. |
| Balances/positions streaming (later) | Once price + ENS streaming are stable, move broker read-model refresh to `/port/v1` subscriptions on the same stream. | Broker snapshots stay current between scheduler cycles without extra REST load. |
| Unified Saxo HTTP client | Consolidate the several per-module `saxo_get_json` implementations (markov_method, saxo_portfolio, daily_indicators, auth) into one shared client module: a single long-lived `reqwest::Client` (today several call sites build a new client per request, defeating connection reuse), HTTP/2 with gzip/deflate `Accept-Encoding` (Saxo explicitly recommends HTTP/2 over the now-obsolete batch endpoints; chart payloads are the big win), uniform 429 backoff (currently only the Markov path retries rate limits), and a per-request correlation id logged with each call. | Fewer TLS handshakes and 429 failures during the ~200-call Markov/indicator runs; one place to tune timeouts and observe Saxo latency. Do not implement multipart batching — Saxo marks it obsolete in favor of HTTP/2. |
| Rate-limit-aware throttling | Build the documented limits into the unified client: 120 requests/minute per session per service group (the nightly Markov run's ~200 sequential chart calls at 500 ms spacing run exactly at this limit — pace to ~100/min per group via a token bucket), 1 order/second per session (space execution-queue placements >=1.1 s apart), and adaptive slow-down using the `X-RateLimit-Session-Limit/Remaining/Reset` and `X-RateLimit-AppDay-*` response headers instead of fixed sleeps. Send a unique `x-request-id` on every POST/PATCH: Saxo returns 409 for identical requests within 15 s without one, which is also the documented idempotency mechanism for safe order-placement retries. Note: a future entry + related stop/target feature must bundle the orders in one request because separate placement is rejected. | Zero avoidable 429/409 failures; Markov and indicator runs finish faster with header-driven pacing; order retries become idempotent by construction. |
| Order-placement return codes and disclaimers | Fold the learn-section "Order Placement return codes" and "Pre-Trade Disclaimers" material into the Saxo error taxonomy row so precheck/placement failures map to documented codes rather than string matching. Per the planned-changes page, pre-trade disclaimer handling is mandatory for all OpenAPI apps (enforced from May 2025; orders can be rejected when an app does not handle disclaimers even if none currently apply) — the runtime has no disclaimer handling today, so implement the check-and-confirm flow around order placement; SIM apparently does not enforce it yet, LIVE presumably will. Also from planned changes: `root/v1/user` retires 2026-09-01 (not used by this runtime — verified 2026-07-11), and the 2026/2027 client-management/proxy-voting changes do not apply. | Orders cannot start failing with disclaimer rejections on LIVE (or a future SIM enforcement wave); deprecations are tracked with evidence instead of discovered as outages. |
| SIM limitations note + LIVE readiness checklist | Per the environments page, SIM is a restricted copy of LIVE: some market data and reporting features are unavailable, support priority is lower, and SIM may run newer API versions than LIVE — which sets expectations for known SIM quirks (delayed quotes the limit-offset logic already assumes, reference-data lag such as the pending SPCX listing). Keep a wiki checklist for an eventual LIVE migration: separate application registration (app key/secret are not shared between environments), redirect URIs re-registered on the live app, live auth/gateway/streaming hosts (`live.logonvalidation.net`, `gateway.saxobank.com/openapi`, `live-streaming.saxobank.com/oapi/streaming/ws` — the code switches REST hosts on `saxo.environment` already, streaming must do the same), new k8s secrets, market-data entitlement costs, `require_approval_live: true` re-enabled, and a full safeguard verification pass (breaker, floors, quarantine) before any real order. | SIM oddities stop being investigated as bugs; a LIVE switch becomes a checklist instead of an improvisation. |

## Decision And AI Pipeline

The decision-report pipeline should become more deterministic, easier to diagnose, and cheaper to iterate.

- Split report generation into stages: build context, submit model request, parse/normalize response, persist report, run manager, process execution queue.
- Landed 2026-07-31: persist diagnostics in a bounded debug view. AI Prompts now fetches sanitized prompt, request, provider-response, and normalized-report payloads only after expansion; each is capped and redacted server-side before the browser sees it. Follow-up: retain only the minimum debugging horizon required by operations.
- Add a provider capability matrix for OpenRouter models: strict schema support, plugin support, timeout profile, cost, and observed parse reliability.
- Add fallback behavior for provider/schema failure: keep the failed report but optionally retry with a known-safe model and schema after operator approval.
- Add model comparison runs that produce reports without queueing trades, so Hermes can evaluate decision quality across models.
- Add report-quality checks before Trading Manager sees a report: active market scope, suggested trade count, valid strategy keys, valid currencies, cash-budget consistency, and required Markov/daily-indicator evidence.
- Landed 2026-07-25: Added the first bounded external-research ingestion path for the public App Economy Insights RSS feed. The Rust scheduler caches sanitized public-feed metadata and summaries, deduplicates it, matches only explicit configured aliases, and supplies it as attributable secondary context to Decision Reports and Hermes. It does not scrape/paywall-bypass, change Trading Manager gates, or alter broker execution. Remaining work: Watchlist cards and report-level usefulness measurement.
- The editorial-research boundary is hardened (see the P0 Security row and [urgent-todo](urgent-todo.md) U4). Next, add per-report usefulness measurement: record which items were attached to a report, whether the model cited one in a rationale, and whether cited items correlate with better or worse outcomes, so the feed catalog grows on evidence rather than availability.
- Port the existing legacy `market_data.rss` source catalog into the active Rust public-feed framework, beginning with the configured Yahoo Finance RSS index, CNBC, Reuters, and public macro feeds. Validate every feed's terms, availability, attribution, rate/cache policy, parsing quality, and ticker-match precision; do not scrape Yahoo Finance quote pages or treat a configured feed as an active trading input until those checks and bounded persistence are complete.

## Hermes Self-Improvement

Hermes should become a useful advisory layer that learns from evidence without becoming an unbounded actor.

- Add a Hermes advisory dashboard section with rows for each report: input report id, advice status, recommendation, orders allowed/reduced/blocked, timeout status, and final Trading Manager outcome.
- Add "proposal impact" tracking: when Hermes blocks or reduces an order, follow the hypothetical outcome versus the executed path.
- Give Hermes access to normalized report outcomes: report completed, manager queued, broker submitted, filled/expired/failed, and next-day/weekly P/L attribution.
- Landed 2026-07-23: Added a bounded, read-only "Lessons Pending Review" queue derived from recent Hermes reflection `proposed_actions`. It collapses duplicate normalized action text to the newest reflection and exposes only safe operator context. It does not persist a second workflow, approve experiments, change strategy config, or affect broker behavior.
- Landed 2026-07-23: Added a read-only "One-Variable Audit" that separates promoted baseline audit records from the exact supported paper/SIM overlay the Trading Manager would select, including latest-run observation. It cannot rewrite configuration, transition experiments, or activate live behavior.
- Landed 2026-07-23: Added a deterministic, read-only "Proposal Quality Review" for active/pending experiments. It scores safe metadata and surfaces evidence, measurement, safety, and duplicate-review gaps without exposing raw proposal content or changing lifecycle state.
- Landed 2026-07-23: Added bounded, read-only "Learning Memory" derived from safe reflection actions. Repetition across two distinct reflections becomes `stable`; one-offs remain `emerging`; lessons expire after 7 or 21 days. Stale rows stay visible for audit but are excluded from Hermes advisory context, and the projection cannot mutate strategy, lifecycle, configuration, or broker behavior.
- Exact duplicate detection landed 2026-07-10 and is shared by the protected HTTP adapter and Hermes MCP tool as of 2026-07-22. A deliberately small, explicit related-family map now flags the two cash-buffer paths as advisory review context before insert; it does not auto-merge, reject, approve, or activate a different variable. Remaining work: add another family only with evidence that its paths are truly interchangeable, then keep the final lifecycle decision with the operator.
- Landed 2026-07-23: Added a read-only baseline promotion evidence pack for the active baseline. It reports only exact-overlay manager/report/order activity plus bounded experiment and post-promotion portfolio observations (return, drawdown, cash utilization, and sufficient-sample zero-risk-free Sharpe); all values are explicitly non-causal and cannot affect lifecycle or broker behavior.
- Keep conservative mode as default for report-time advice; only consider stronger modes after dry-run, paper, and SIM evidence is strong.

## Trading Manager And Execution

The execution path should be more transparent and more resilient before expanding live behavior.

- Finish Rust parity for broker status sync, replace/cancel management, fill reconciliation, and local ledger reconciliation.
- Add a queue state machine with explicit statuses: `queued`, `precheck_ok`, `broker_working`, `partial_fill`, `final_fill`, `expired`, `cancelled`, `rejected`, `local_reconciled`.
- Store normalized broker failure categories alongside raw error text: auth, market closed, instrument not tradable, commission setup, insufficient cash, tick size, quantity, session expired, unknown.
- Add order expiry handling for day orders: show expected expiry time in local exchange time and reconcile expired broker orders promptly.
- Add price/tick-size diagnostics directly into execution events for limit orders.
- Add broker-hosted protective-stop lifecycle for filled BUY positions. The manual SIM-only lifecycle-test workflow and explicit unprotected-position exceptions landed 2026-07-25; next, an operator must perform and review an explicitly approved small-SIM placement/cancel/reconciliation test, then separately design parent/child linkage. Default to Stop rather than StopLimit so a fast decline cannot leave a triggered but unfilled protective order. A stop cannot guarantee the execution price through a market gap.
- Landed 2026-07-21 and completed 2026-07-26: Execution attribution links order -> report -> Trading Manager -> Hermes -> report-time Markov/technical/capital context. It prefers stored manager snapshots and labels current latest-signal reads as a fallback for historical records. Reconciled fill-ledger totals add SELL realised P/L/costs or BUY position-book outcomes, while daily-close and observed cross-order lifecycle evidence complete the current post-fill mechanics. The next step is a durable trade thesis at entry, not a broker-mutation change.
- Add a broker-safe simulation mode that runs all prechecks and manager decisions but never places orders.

## Strategy, Markov, And Risk

Strategy changes should be evidence-driven and reversible.

- Promote the pending confluence-count experiment only after comparing missed trades, avoided failures, and P/L impact.
- Promote or reject the Markov signal age gate after measuring stale-signal decision quality.
- Add per-symbol regime history charts: current Markov state, transition matrix, stationary distribution, signal, and signal age.
- Landed 2026-07-27: Currency and exchange concentration gates are enforcement-ready under `strategy.concentration`. They use only canonical symbol suffixes and the local exchange-to-currency map, are retained in manager/Hermes/waterfall audit data, and default to explicit unlimited (`0`) until an operator supplies limits. The previous unsupported `strategy.max_assets_per_sector` setting was retired from shipped configuration, the contract, and the legacy reference selector; it never affected the active Rust runtime. Reintroduce sector concentration only after choosing a durable sector source, such as Saxo `/ref/v1` sector/industry fields or a maintained symbol-to-sector map, and defining its exposure semantics.
- Landed 2026-07-27: Added a bounded, read-only Holding Thesis Reviews queue. It joins the latest broker-confirmed local position snapshot with the most recent recorded BUY thesis, flags stale decision evidence after `strategy.swing.position_decision_stale_after_days` (7d) and the recorded `next_2_weeks` window after 14d, and reaches Hermes as sanitized review context. It is not a SELL recommendation, manager gate, sizing input, or broker action; imported lots and later position changes can make the entry only partial provenance. Follow-up: require a mature observation plan before deciding whether any thesis-invalidation rule should affect an order.
- Quantify and document weekend and off-pulse gap exposure. Scheduled reports run only on the two weekday open-followup pulses, so an exit decision made Friday afternoon cannot act before Monday, and no exit exists between pulses on any day. Until protective stops land ([urgent-todo](urgent-todo.md) U1), this is the system's real maximum unmanaged holding interval; measure it against realized overnight and weekend gaps before deciding whether stops, a Friday risk-reduction pulse, or both are the answer.
- Landed 2026-07-27: Added bounded Decision Pulse Outcome Evidence across EU open, US open, manual/manual dry-run, portfolio sync, and legacy report-linked orders. It separates BUY forward directional movement from local-ledger SELL realised DKK results, and uses the persisted Trading Manager advice-delta snapshot for per-order Hermes effect coverage. This is classification, not causal evidence. Follow-up: define a mature-sample analysis plan before comparing Hermes-effect subgroups, and extend eventual-exit/accounting evidence after history exists.
- Landed 2026-07-27: Cash Deployment now retains and displays a compact, ranked list of stable Trading Manager gate codes for skipped BUY candidates, alongside its existing policy, candidate, approval, and blocked counts. It distinguishes concrete observed blocks such as `cash_budget` or `market_open` without inferring unrecorded causes. This is read-only observability: it cannot alter selection, capital policy, Hermes advice, or broker behavior. Decision Pulse now separately shows persisted per-order Hermes effects and local broker/queue states. Follow-up: add bounded source-specific attribution for conservative Hermes and stale signals only where the manager run records those causes.
- Landed 2026-07-26: drawdown guardrails that reduce buy budget or require review after threshold breaches. **Live status 2026-08-02: 18.999% against a 20% halt** — see [urgent-todo](urgent-todo.md) U9. Follow-up the guardrail design deliberately left open: there is no re-entry rule. At the halt, SELLs continue and BUYs stop, so a sustained breach converts the book to cash with nothing defining how it resumes. Decide whether resumption is time-based, recovery-based, or operator-only *before* the first halt rather than during it.
- **Treat currency as a strategy dimension, not just a reporting one.** The book is 63% USD against a DKK reporting currency, and USD/DKK fell 7.66% in the month to 2026-08-02 — roughly a quarter of the observed drawdown, from an exposure no gate bounds, no signal source observes, and (until [urgent-todo](urgent-todo.md) U10) no report measured. The `strategy.concentration` currency gate landed 2026-07-27 but defaults to unlimited. Sequence: measure it first (U10), then set a limit with evidence, then decide whether hedging is in scope at all. A DKK-based book buying US equities is taking a currency position whether or not it intends to.
- **The Hermes experiment queue produces proposals nobody decides on.** Live on 2026-08-02: four experiments sit `pending_review` (2026-07-03, 07-10, 07-24, 07-29) and one has already reached `expired_stale` without ever being judged. Daily advice is flowing normally (2 `received` per weekday), so the advisory half works; it is the experiment half that has no closing step. A proposal that expires unreviewed is worse than no proposal — it consumes a reflection cycle and teaches the loop nothing. Either give review an owner and an SLA, or make promotion automatic under the existing `promote_only_if` contract and let the guardrails be the reviewer. The month-old backlog suggests the current design assumes an operator attention budget that does not exist.
- **Establish whether the strategy has an edge before adding more signal sources.** Seventeen closed round trips since the Rust cutover: 2 winners, 15 losers, +1,187 DKK net — the entire result is one Alphabet trade (+36,355). n=17 is too small to conclude anything, which is precisely the point: the system currently has more signal inputs (Markov, Quiver, daily indicators, support risk, editorial research, Hermes advice) than it has closed trades to evaluate them with. Prefer work that increases the rate of evaluable outcomes or the quality of existing signals over work that adds new ones. See [urgent-todo](urgent-todo.md) *Realised Outcomes*.
- ~~Investigate 1–2 day round trips~~ **Investigated 2026-08-03: mostly not what it looked like.** The three flagged (`AJG` 07-29→07-30, `JNJ` 07-28→07-30, `DSV` 07-21→07-23) looked like the model contradicting itself within 48 hours. Checked `execution_orders.strategy_type` for each closing SELL rather than assuming: `AJG` and `JNJ` both closed via `protective_stop` — the ATR stop firing correctly on a losing position, exactly the behavior U1 was built for, not churn. Widening the query to every fast close in the trading history (correctly paired to each symbol's *most recent* preceding BUY, not the symbol's lifetime-first BUY) found 9 total: 3 more (`LMND`/`AJG`/`ZTS`, 2026-05-07) were one coordinated portfolio-wide flatten from a single report (`report_id: 9`) two days after the initial 2026-05-05 launch import — a one-time bootstrap event, not organic trading. 3 more (`PLTR`/`ASML`/`AMAT`, 2026-06-10) were `strategy_type: manual` — operator-placed, not automated at all, so a churn guard on automated re-entry wouldn't even apply to them. That leaves exactly **one** genuine same-week discretionary reversal in the entire history: `DSV`, closed on an explicit fresh-bearish-Markov-regime rationale two days after a momentum-driven entry. **Conclusion: do not build a cooldown/churn guard on this evidence.** n=1 is not a pattern, and two of the three original examples were the safety system working, not failing. Revisit only if discretionary same-week reversals (excluding protective-stop closes and manual orders) recur — track the `strategy_type` split going forward rather than raw holding-period alone, since that is what separates "the model changed its mind" from "the stop did its job."
- Add explicit "do not trade" lists for instruments that repeatedly fail precheck, instrument resolution, commission setup, or market-scope checks.
- Landed 2026-07-25: read-only multi-horizon support-risk context from daily Saxo OHLC data. The current model uses clustered pivot-low zones from up to 1-year and 5-year windows and exposes nearest/next support, downside measures, break risk, confidence, and actual history coverage in the Decision Report, Hermes context, and Watchlist. Follow-up: add volume-aware zone evidence only when a reliable daily-volume source is available; no trading decision currently depends on this projection.
- Continue the multi-horizon support-risk evaluation after sufficient collection: compare held-out outcomes for candidates near support, candidates breaking support, and a no-overlay baseline; include costs/gaps where a suitable data source exists. Require a one-variable Hermes/SIM proposal with explicit sizing or review-only effect before any promotion. Do not treat chart levels as deterministic floors or targets.

## Portfolio Snapshot Schema

`portfolio_value_history` stores only aggregates — one row of totals per snapshot, with no record of which positions produced them. That choice is why the 2026-06-03 → 2026-07-09 cost-basis corruption is **permanently unrepairable**: roughly 6,300 rows hold a `total_cost_basis_dkk` of 5.4M–26.9M DKK against ~240k invested, and there is no per-position detail to recompute a correct total from. The same limitation blocked the unrealised-P/L backfill after the 2026-08-06 broker-currency bug — that column had to be left wrong too.

The pattern is worth naming: **an aggregate-only snapshot converts any upstream arithmetic bug into permanent history.** Both incidents were fixed at source within days, and both left a scar that cannot be removed. A third will do the same.

**Proposal: `portfolio_position_snapshots`, a per-position sibling table.**

| Column | Purpose |
| --- | --- |
| `snapshot_id` | FK to the `portfolio_value_history` row |
| `symbol`, `currency`, `quantity` | Position identity and size |
| `price_local`, `fx_rate_to_dkk` | The two inputs every DKK figure derives from |
| `cost_basis_local`, `cost_basis_dkk` | Both sides, so a bad conversion is detectable by ratio |
| `market_value_dkk`, `unrealised_pnl_dkk` | Stored derived values, recomputable from the above |

The property that matters: **every stored DKK figure is reproducible from `quantity × price_local × fx_rate_to_dkk`.** A future conversion bug becomes a backfill rather than a scar, exactly as the 2026-08-06 FX-split repair was possible only because `trade_ledger` already stored both rates per row. That precedent is the argument — the ledger got this right and was repairable; the snapshot table did not and was not.

**Sizing.** ~145 snapshots/day × ~19 positions ≈ 2,750 rows/day, ~1M/year. That is larger than everything else in the database combined, so this needs a retention policy from the start, not added later: keep per-position detail for a bounded recent window (90 days is the drawdown lookback and a natural fit) and thin older snapshots to daily closes. `scheduler_cycle_history` already establishes the prune-on-cycle pattern to copy.

**Sequencing, smallest useful slice first.**

1. Create the table and write to it alongside the existing aggregate. Change nothing that reads. This alone stops the bleeding — from that day on, history is repairable.
2. Add the retention prune to the scheduler cycle, before volume becomes a problem rather than after.
3. Add an integrity check comparing each snapshot's aggregate against the sum of its positions. A divergence is a bug caught in hours instead of a month.
4. **Landed 2026-08-23:** Performance now makes the repairable boundary visible with selected-range coverage, retained-detail counts, and the bounded aggregate/detail integrity state. Its first consuming historical projection is the latest retained composition, explicitly timestamped and separated from the live broker portfolio. Keep chart and valuation reads on their established aggregate semantics unless a future use case needs another specific per-position historical projection.

**Deliberately not in scope:** repairing the existing corrupt window. It stays unrepairable regardless — this prevents the *next* one. The 2026-06/07 range is marked in the Performance view instead (landed 2026-08-06).

## Portfolio And Performance Analytics

Performance should be explainable at portfolio, report, and position level.

- [x] Add a read-only reconciled SELL-outcome panel from local ledger rows: show realised P/L, win/loss/breakeven counts, win rate, payoff ratio, commission, tax, and an explicit collecting/preliminary sample label. It also attributes realised outcomes by symbol and instrument currency without claiming FX attribution. Partial sales are rows rather than synthetic round trips; defer holding-time and realised-slippage metrics until lot-to-sale and broker quote-at-submission evidence are stored.
- Reconcile dashboard aggregate P/L with broker exposure rows and local portfolio value history, and show source freshness for each.
- Add P/L attribution by symbol, currency, sector, and strategy role. Symbol and
  instrument-currency attribution plus recorded SELL exit-route provenance
  landed 2026-07-31. Exit routes describe only the linked SELL order and keep
  unlinked or ambiguous ledger rows explicit; they are not entry-strategy
  attribution. Sector attribution remains deferred until a durable source and
  exposure semantics exist.
- Add daily, weekly, monthly, and since-reset performance cards with target progress and drawdown.
- Add benchmark comparison against relevant indices for active regions. Landed
  2026-07-30 with Saxo-SIM-validated S&P 500, Nasdaq-100, Dow Jones, MSCI
  World, MSCI Europe, and FTSE 100 ETF proxies. Follow-up: preserve the
  price-return/FX/cash comparability caveat, and only add another benchmark
  after a named-index and Saxo-SIM validation check.
- Add trade expectancy metrics once enough closed trades exist: win rate, average win/loss, payoff ratio, holding time, and slippage.
- Add cost tracking: commissions, FX impact, spread assumptions, and slippage versus limit/market price.
- Add "performance confidence" labels when market data is delayed, approximated, stale, or missing.
- Landed 2026-07-26: estimate Danish share-income tax from the configured 27%/42% brackets. The after-tax summary reports estimated tax separately and applies incremental bracket treatment to realized YTD gains plus the current unrealized result. This is a portfolio-planning estimate rather than a tax return: it does not model every individual allowance, loss carry-forward, dividend withholding, FX treatment, or filing detail. Follow-up: preserve the estimate label and do not feed it into trading gates without a separately approved accounting model.

## UI And UX

The dashboard should make action, risk, and system state obvious without needing logs.

- Add a persistent top status strip for Saxo session, scheduler, latest EU/US report, latest Hermes advice, Markov freshness, and execution queue health.
- Add disabled/loading states for all long-running actions, with progress text and final success/failure detail.
- Add per-tab stale-data timestamps and "source" labels.
- Add report detail pages with tabs for prompt, response, normalized report, manager result, Hermes advice, and orders.
- ~~Add execution order detail modals with timeline events and broker error details~~ Landed 2026-08-04 as an inline expandable timeline rather than a modal — the Execution row already carries the order's own fields across 16 columns, so what was missing was only the broker lifecycle, and an expandable cell keeps it next to the row it describes instead of behind a dialog. Broker error details were already present in the Error column with the failure-stage taxonomy. `GET /api/execution/orders/{id}/events` loads on demand per order, following the existing `data-decision-debug` pattern. **Per-order rather than filtering the dashboard's flat event list client-side**: that list is capped at 50 rows, so any order older than the most recent handful would have rendered an empty timeline and looked like an order that never reached the broker. The endpoint's column list is an allowlist — `raw_payload_json` (unredacted Saxo responses) and `account_uid` are excluded, with a test asserting neither can be served and that adding a table column cannot silently add it to the response. Verified live: order 285 renders `queued_by_trading_manager` 14:55:42 → `submitted_to_broker` 14:55:43 → `broker_final_fill` 14:55:59 (FinalFill · Confirmed · qty 6 · @ 193.12), no console errors, and reopening does not re-fetch.
- ~~Add a Hermes tab split into Overview, Advice, Reflections, Experiments, and Baselines~~ Landed 2026-08-04, with **per-section data loading**, not just visual grouping. The tab rendered nine sections in one 335-line scroll and ran eleven separate queries on every load regardless of what was being viewed; each dataset is now gated to the one section that renders it, extending the per-tab lazy-read policy a level deeper. Measured live: the default Overview is **22.5 KB** against roughly 327 KB for the combined sections, about a 93% reduction on first load (Advice 165 KB and Reflections 88 KB are the heavy ones, now paid for only when opened). Sections are plain links carrying their own query string, so each has a shareable URL. **The header pills are gated the same way** — rendering them all would have printed "Reflections: 0" while sitting on Baselines, and a zero meaning "not loaded" reads as "none exist", which is worse than the problem being fixed. A test asserts every dataset gates to exactly one section: gated to none renders a permanently empty table, gated to several defeats the split.
- ~~Add a Markov tab table filter for portfolio, watchlist, errors, stale signals, and high-conviction signals~~ Landed 2026-08-04: server-side filters (All / Portfolio / Watchlist / High conviction / Errors) as plain links carrying their own query string, so they work without JavaScript and every filtered view has a shareable URL. The page and count queries share one `markov_filter_sql` function deliberately — divergent predicates would make the pagination control advertise pages that render empty, which is worse than no filter — and the paging links carry the active filter for the same reason. "High conviction" compares against `strategy.swing.markov_gate.min_signed_signal`, the threshold the Trading Manager actually applies, so it means "would clear the gate" rather than a display cutoff. Verified live: All 201, Portfolio 18 (matching the 18 held positions exactly), Watchlist 183, Errors 1 (SPCX), and 18+183=201 confirming the two are exact complements on real data. **"Stale signals" was deliberately not implemented**: every row in a run shares that run's `run_date`, so staleness is a property of the run, not of one signal against its siblings — a per-row stale filter would match everything or nothing. Signal *age* is already surfaced at the run level.
- Add keyboard-safe and mobile-safe layouts for the main monitoring views.
- Per-tab lazy read models (see P0 UI performance row): each view fetches only its own data, heavy JSON/prompt payloads load on demand, and long tables (positions, Markov signals, execution orders, scheduler cycles) paginate server-side. Target: any tab under 300 ms server time and under 200 KB HTML.
- ~~Age-label or hide stale per-position decisions~~ Landed 2026-07-14: portfolio and watchlist decision chips show relative age, become `Stale` after the configurable seven-day default horizon, and treat missing timestamps as stale rather than current advice.
- ~~Make ops-banner staleness market-aware~~ Landed 2026-07-14: weekday-only Markov, Quiver, and Indicator jobs render neutral `idle (weekend)` or `waiting` when no run is due. A warning now means a run missed its latest scheduled due date, failed, or completed only partially.
- ~~Defer embedded TradingView charts until selected~~ Landed 2026-07-23: watchlist sparklines now render an `about:blank` iframe and attach the external advanced-chart URL only when its modal opens. This removes background chart requests from every hidden row and leaves the selected chart's remaining load time to TradingView.
- ~~Make Watchlist quote provenance legible~~ Landed 2026-07-23: the table now separates successful Saxo quote-monitor data, configured-but-unenriched universe membership, decision snapshots, broker snapshots, partial runs, and stale history. Labels include compact hover explanations; opaque internal status codes are no longer rendered as the operator-facing state.
- ~~Render the AI Prompts view as collapsed sections instead of a 1 MB inline dump~~ Already landed by the time of the 2026-08-03 review: `AiPromptsView` (`src/ui.rs`) loads the Decision Report prompt/request/response only inside an expandable `<details>` panel, server-sanitized and capped before it reaches the browser — matches the existing P0 UI performance row's note that "AI Prompts now loads server-sanitized debug payloads only after an operator expands them." This bullet had gone stale; no copy-button affordance exists yet if that specific convenience is still wanted.

## Refactoring And Architecture

The Rust runtime should keep moving away from generic JSON and legacy Python behavior references.

- Replace generic JSON dashboard payloads in `src/state.rs` with typed models in `src/models.rs`.
- Split large state builders into focused read-model modules: portfolio, reports, execution, Hermes, Markov, scheduler, settings. Seven bounded M4 slices landed 2026-07-31: `src/hermes_state.rs` owns Hermes evidence projections, `src/performance_state.rs` owns pure account-value projections, and `src/markov_state.rs`, `src/quiver_state.rs`, `src/execution_state.rs`, and `src/scheduler_state.rs` own dashboard page bounds while their source modules retain persisted reads. `AppState` retains orchestration, database queries outside those boundaries, and all lifecycle/mutation functions. `src/state.rs` remains the largest module and every new feature must avoid adding unrelated read-model logic there. The public Performance snapshot-evidence envelope was typed 2026-08-23; next replace another small compatibility-JSON boundary only where existing API-payload tests preserve behavior.
- Introduce typed decision-report structs and typed Trading Manager structs instead of ad hoc JSON traversal.
- Move OpenRouter/OpenAI provider code into a provider module with a shared request/response abstraction.
- Move report schema construction into a dedicated module with schema tests.
- Keep `src/main.rs` startup-only and avoid adding business logic there.
- Continue removing Python runtime dependencies from active Kubernetes images. **The "keep legacy Python as behavior references" position expired on 2026-08-02** — see the Python Removal Plan in [urgent-todo](urgent-todo.md). The Rust implementations are now ahead of the Python ones, so `AGENTS.md:100-118` pointing agents at `src/saxo_daytrader_xai/api/app.py` as "the reference for API behavior" actively misleads. 91 tracked files / 28,353 lines remain; only the two Postgres backup scripts are live. One genuine exception before deletion: `tax_engine.py:273` holds the repository's only correct FX-attribution formula, still unported (U10).
- **SQL: statistics, retention, and payload size.** Three findings from the 2026-08-02 production review, none currently causing visible slowness and all of the kind that stop being cheap later. (1) Every `last_autoanalyze` dates to 2026-06-30, so the planner believes `audit_log` has 0 rows where it has 67,578 — `ANALYZE` plus a lower `autovacuum_analyze_scale_factor` for append-heavy tables. (2) `audit_log` is 65 MB, 38% of the 172 MB database, written only by the retired Python runtime and read by nothing. (3) `decision_reports` averages 205 KB/row and `prompt_text` alone has grown 271 KB → 429 KB → 527 KB across May/June/July; the bulk is Markov `recent_labels` (1,240 raw daily observations per prompt) that the model does not need. Also note `trade_ledger` carries no index beyond its primary key while being queried by symbol, side, and date for cost-basis and cash-flow lookups — harmless at 118 rows, not harmless later.
- Add repository-level architecture decision records for major porting choices.
- ~~Decide the fate of the `frontend/` Next.js app versus the Dioxus SSR dashboard~~ Resolved 2026-07-04: the Next.js app was legacy (never built or deployed) and was removed; the Dioxus SSR dashboard in `src/ui.rs` is the committed UI. Note the `daytrader-frontend` Kubernetes Service is unrelated to that directory — it is a live alias Service selecting the API pods that the shared ngrok AgentEndpoint routes through, and must be kept (or renamed only together with the gateway route).
- Scheduler advisory-enrichment timeout budgets landed 2026-07-26 for Markov, daily indicators, Quiver, and editorial research. They record a terminal timeout result and retry on the next cycle; timeout policy deliberately excludes broker/session/order, protective-stop, decision, manager, execution, and reconciliation mutations because their partial completion must be reconciled rather than cancelled.

## Operations And Deployment

Local Docker Desktop Kubernetes should stay easy to inspect and recover.

- Add alerting for repeated decision-report failures, repeated broker execution failures, stale scheduler heartbeat, and missed EOD reflection.
- Keep public ngrok route ownership in the shared gateway and app-owned internal AgentEndpoint ownership in this repo.
- Add watch-symbol lifecycle alerts: when an `extra_symbols` entry (e.g. SPCX, waiting for Saxo SIM to sync the 2026-06-12 IPO) resolves for the first time, send a Slack notice and record the activation, instead of silently starting quotes. Include the inverse alert when a previously resolvable symbol disappears from reference data.

### Build, Deploy, And Repo Hygiene (reviewed 2026-07-11)

| Idea | Shape | Measurement |
| --- | --- | --- |
| Docker dependency-layer caching | Landed 2026-07-23: `Dockerfile.api` first compiles a dummy `main` after copying the manifest, lockfile, and tracked revision build script, and uses BuildKit cache mounts for the Cargo registry and target directory. The Git SHA is injected only before the final source build, retaining dependency artifacts while recompiling revision metadata. | Verified cached rebuild reuses dependencies while revision changes recompile the application metadata. |
| Build-context hygiene | Landed 2026-07-23: screenshot directories are excluded from Docker context. | Cached image build transferred roughly 16 KB of source context in validation; screenshots remain outside deploy payloads. |
| Continuous integration | Landed 2026-07-25: `.github/workflows/ci.yml` runs `cargo fmt --check`, `cargo check --all-targets`, and `cargo test` with `RUSTFLAGS: -D warnings` on push to `main`, on pull request, and on manual dispatch. Hermetic by design — no secrets, no Saxo or OpenRouter access, no deploy step; the whole 324-test suite completes in well under a second because database-backed fixtures use isolated in-memory SQLite. Pairs with the content-addressed image tag row below: deploy provenance proves the intended commit shipped, CI proves that commit was green. Verified green on its first run (2026-07-25, commit `0ff0c8f`). Note the dirty-tree guard already existed: `scripts/deploy_k8s_docker_desktop.sh` has refused a dirty worktree since deploy provenance landed. The remaining gap was that nothing proved a commit was green *before* it shipped, so `make release` now chains `validate` -> `k8s-deploy` -> `post-deploy-guard`. | No untested commit reaches a deploy; the 2026-07-09 stale/dirty-tree deploy that silently dropped the circuit breaker for a trading day becomes detectable at push time rather than in production. |
| Content-addressed image tags | Landed 2026-07-26: the deployment script now resolves and validates the full clean-worktree Git SHA before assigning default image names, then tags API and backup images with that immutable SHA. `IMAGE_TAG`, `API_IMAGE`, and `BACKUP_IMAGE` are explicit overrides only. `.run/last_deploy.env`, Kubernetes deployment images, and binary build metadata now share the same source identifier. CI also syntax-checks the deployment and post-deploy scripts. | Every default running image is traceable to the exact commit that produced it; guard metadata is self-explanatory. Skip-build-when-unchanged remains a separate deployment-speed optimization. |
| Deploy script speedups | `deploy_k8s_docker_desktop.sh` runs `helm upgrade cnpg` and full kustomize apply on every deploy and then waits for four rollouts serially. Skip the CNPG helm step unless the chart/values changed (version pin + hash check), run the rollout waits in parallel, and short-circuit restarts when the image digest is unchanged. | Routine deploy wall-time drops by roughly half; fewer pointless pod restarts (also reduces Saxo session-burn exposure until the refresh lease lands). |
| Legacy Python surface removal plan | The retired Python runtime still occupies the tree: `main.py`, `web_main.py`, `src/saxo_daytrader_xai/` (1.7 MB), `requirements.txt`, `.venv/` (425 MB), `.pytest_cache`, and legacy `scripts/*.py` — while `scripts/create_postgres_backup.py` and `prune_postgres_backups.py` remain genuinely used by the `daytrader-backup` CronJob image. Enumerate which Python files are load-bearing (backup scripts, `saxo_oauth_helper.py`?), delete the rest in one commit, and port the two backup scripts to the Rust binary later so `Dockerfile.backup` can retire too. | AGENTS.md "legacy surface" section shrinks to the backup scripts only; no more agent time spent reading dead Python for behavior reference. |
| Working-tree data cleanup | Non-code data sits inside the repo working tree: `rustfs/` holds 12 GB of live RustFS object-store data (gitignored but one `rm -rf` away from destroying backups), root-level `Positioner_*.csv` exports (personal portfolio data; the 17-maj file is the verified source of the 2026-07-08 cost-basis repair), legacy `ledger.db`, and empty `minio-data/`/`logs/`/`data/` dirs. Move the RustFS data directory outside the repo (it is an external service's storage), archive the 17-maj CSV under `data/` with a README pointing at the repair log, delete the other two CSVs and `ledger.db`, and prune the empty dirs. | The repo tree contains code and docs only; backup storage cannot be lost to a careless repo clean; personal exports stop living at the repo root. |

## Security And Secrets

Security posture should assume model prompts, broker payloads, and external docs are untrusted.

- Run dependency and CVE hygiene regularly with `make deps-dry-run` and `make security-scan`; treat fixed HIGH/CRITICAL CVEs, RustSec advisories, and secret findings as release blockers unless there is a dated exception with a compensating control.
- Keep all Saxo tokens, AccountKey, ClientKey, OpenRouter keys, ngrok keys, and database credentials out of wiki pages and normal UI payloads.
- Add automated redaction tests for logs, report debug views, Hermes context, and MCP tool responses.
- Make Hermes capabilities explicit and deny broker mutation tools by default.
- Add role separation in UI for view-only operations, report generation, order processing, settings changes, and experiment promotion.
- Add audit records for settings changes, experiment transitions, manual report triggers, and manual queue processing.

## Knowledge And Documentation

The wiki should become the durable operating memory for both Codex and Hermes.

- Add a short "current production shape" page that states active namespace, endpoint routing, storage backend, AI provider, model, and safety modes.
- Add runbook pages for common incidents: OpenRouter schema failure, Saxo reauth required, broker order rejected, Markov stale, CNPG degraded, RustFS unavailable, ngrok route broken.
- Add a decision record when enabling or rejecting each Hermes experiment.
- Keep [schema](schema.md), [log](log.md), and [runbooks/build-test-deploy](runbooks/build-test-deploy.md) current after operational changes.
- Use `qmd` to query the wiki before making architecture or strategy changes.

## Suggested Milestones

| Milestone | Outcome | Candidate Scope |
| --- | --- | --- |
| M1: Safe Observability | Operators can see why the system did or did not trade today. | Status strip, report pulse cards, execution failure details, Hermes advice audit table. |
| M2: Non-Mutating AI Test Harness | AI/schema/model changes can be tested without queueing orders. | Dry-run report endpoint, model comparison runs, schema validation tests, sanitized prompt/response debug page. |
| M3: Conservative Hermes Loop | Hermes advice is consistently attached, measurable, and reviewable. | Advice dashboard, conservative enforcement audit, proposal impact tracking, duplicate proposal detection. |
| M4: Typed Rust Core | Critical read/write paths are typed and easier to test. | Typed report, manager, execution, and Hermes models; module split from `state.rs`. |
| M5: Strategy Evidence Loop | Experiments can be promoted or rejected from measured SIM evidence. | Baseline evidence packs, attribution, risk metrics, one-variable audit view. |
| M6: Intraday Shadow Evidence | Later-session Decision Reports can be evaluated without increasing broker authority. | 14:15 Europe/Copenhagen EU and 14:15 America/New_York US shadow pulses, immutable no-queue contract, shadow outcome ledger, tuning dashboard, expanded EOD attribution, and separate promotion record. |

## Open Questions

- Should manual decision reports default to dry-run and require a second explicit action to queue trades?
- Which report models should be allowed in scheduled mode versus manual dry-run mode?
- What minimum evidence should be required before promoting a Hermes experiment to SIM baseline?
- Should broker precheck-only simulation become mandatory before any new instrument class is traded?
- Which UI views need operator authentication levels beyond the existing ngrok OAuth boundary?
